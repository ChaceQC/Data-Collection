[
  {
    "name": "start",
    "content": "研究😀尾",
    "query": "研究",
    "useRegex": false,
    "matchCount": 1,
    "matchesTruncated": false,
    "snippets": [
      {
        "text": "研究😀尾",
        "ranges": [
          {
            "start": 0,
            "end": 2
          }
        ]
      }
    ],
    "highlights": [
      "研究"
    ]
  },
  {
    "name": "middle",
    "content": "前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前研究😀后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后",
    "query": "研究😀",
    "useRegex": false,
    "matchCount": 1,
    "matchesTruncated": false,
    "snippets": [
      {
        "text": "...前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前研究😀后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后...",
        "ranges": [
          {
            "start": 75,
            "end": 78
          }
        ]
      }
    ],
    "highlights": [
      "研究😀"
    ]
  },
  {
    "name": "end",
    "content": "前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前😀尾",
    "query": "😀尾",
    "useRegex": false,
    "matchCount": 1,
    "matchesTruncated": false,
    "snippets": [
      {
        "text": "...前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前😀尾",
        "ranges": [
          {
            "start": 75,
            "end": 77
          }
        ]
      }
    ],
    "highlights": [
      "😀尾"
    ]
  },
  {
    "name": "multiple",
    "content": "中文😀中文末",
    "query": "中文",
    "useRegex": false,
    "matchCount": 2,
    "matchesTruncated": false,
    "snippets": [
      {
        "text": "中文😀中文末",
        "ranges": [
          {
            "start": 0,
            "end": 2
          },
          {
            "start": 3,
            "end": 5
          }
        ]
      }
    ],
    "highlights": [
      "中文",
      "中文"
    ]
  },
  {
    "name": "long",
    "content": "前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前前中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后后",
    "query": "中+",
    "useRegex": true,
    "matchCount": 1,
    "matchesTruncated": false,
    "snippets": [
      {
        "text": "...中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中...",
        "ranges": [
          {
            "start": 3,
            "end": 243
          }
        ]
      }
    ],
    "highlights": [
      "中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中中"
    ]
  }
]
