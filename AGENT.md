# AGENT.md

本文件是“本地资料工作台”项目的开发协作约束。后续进行代码实现、重构、测试、打包、文档维护和 Git 操作时，应优先遵守本文件。

本项目是一个面向 Windows 11 的 Tauri 桌面应用，目标是提供本地文件收纳、检索和预览能力。项目默认不需要后端服务，不上传用户文件，不把云端同步或在线账号作为一期实现前提。

## 1. 基本要求

- 文件读写、终端输入输出和项目文档统一使用 UTF-8。
- 目标平台以 Windows 11 为基线，桌面框架使用 Tauri 2，Rust 使用 stable toolchain。
- 前端默认使用 TypeScript、React 和 Vite；如果后续切换框架，必须同步更新 README 和本文件中的技术基线。
- 项目名称和产品定位统一使用“本地资料工作台”；界面文案、说明文字、代码注释和项目文档默认使用中文。
- 应用采用 local-first 设计。文件内容只在本机处理，默认不上传、不同步、不采集遥测数据。
- 默认只保存文件路径、文件元数据、用户分组和应用设置，不复制、移动或删除用户原文件。
- 任何移动、复制、删除和批量操作都必须由用户明确触发，并在执行前展示影响范围。
- 支持中文文件名、空格路径、长路径和包含非 ASCII 字符的目录；不能假设文件名只包含英文。
- 文件系统路径、用户文档内容、转换器输出和日志中可能包含敏感信息，禁止把文件内容或密钥写入普通日志。
- 读写配置时不得显形输出密钥、访问令牌、私钥或其他敏感配置；示例配置只能使用明显无效的占位值。
- 不要直接使用终端重定向覆盖项目文件；手工修改文本文件使用补丁方式，并保留 UTF-8 编码。
- 不为了“看起来完整”提前引入账号、云同步、在线搜索、复杂权限系统或服务器端 API。
- 预览功能必须使用成熟解析器或渲染器，不手写复杂的 Office、PDF、图片格式解析逻辑。
- 当前一期核心预览格式包括 .md、.txt、.png、.jpg、.jpeg、.webp、.gif、.docx、.xlsx、.doc 和 .pdf。
- .doc 是旧版二进制 Word 格式，不能直接按 .docx 处理；必须通过受控转换适配器实现预览。
- 未完成解析、渲染、错误处理和必要验证的格式，不得在界面中标记为“已支持”。

## 2. 版本规则

- 项目版本号使用 X.Y.Z 形式。
- Git tag 和 Windows 安装包版本使用 vX.Y.Z 形式。
- 开发阶段使用 v0.y.z，第一个稳定版本从 v1.0.0 开始。
- 破坏性行为变更提升主版本号；向后兼容的新功能提升次版本号；修复、文档和内部重构提升补丁版本号。
- 修改文件索引格式、持久化数据结构、预览协议、外部转换器要求、快捷键或安装方式时，必须记录版本影响。
- 应用版本、前端 package 版本、Tauri 配置版本和 Rust crate 版本必须保持一致，不能只修改其中一个位置。

## 3. Git

- 默认主分支使用 main；当前本地仓库已按该分支初始化。
- 独立功能、修复和实验使用 feature/<name>、fix/<name> 或 experiment/<name> 分支。
- 每完成一个可验证的小步就提交，不要积累大量无法定位的混合改动。
- commit message 可以使用 feat:、fix:、refactor:、test:、docs:、chore: 等英文前缀，但冒号后的说明使用中文。
- 提交前必须检查 git status 和改动内容，避免混入无关文件、个人文件、临时转换产物和构建产物。
- 远程仓库不是当前项目的默认前提。未得到明确要求时，不创建远程仓库、不修改 remote、不推送代码。
- 如后续配置远程仓库，默认名称使用 origin，并在 README 或项目进度中记录用途。
- 必须维护 .gitignore，至少忽略：
  - node_modules/
  - src-tauri/target/
  - dist/
  - *.log
  - .env 和其他本地配置文件
  - 应用缓存、临时转换目录和本地测试数据
  - Tauri 安装包、签名私钥和未发布构建产物
- 应提交 package-lock.json、Cargo.lock 和项目需要的其他锁文件。
- 不提交用户真实文档、图片、带个人信息的测试样本、密钥、证书私钥或包含真实路径的调试输出。

## 4. 项目进度记录

项目开始实现后，每次有实际代码、测试、打包或架构变化，都要更新 PROJECT_PROGRESS.md。

必须记录：

- 当前日期。
- 已完成事项。
- 正在进行的事项。
- 阻塞问题或风险。
- 下一步具体任务。
- 涉及的主要文件或模块。
- 已执行的验证方式和结果。

下一步必须写清楚紧接着要做的具体工作，不能只写“继续完善”或“后续优化”。

推荐格式：

~~~
## 2026-08-28

### 已完成

- 完成 Tauri 项目骨架和本地 Git 初始化。

### 进行中

- 实现文件拖放和预览文件类型识别。

### 阻塞与风险

- .doc 预览依赖 LibreOffice 转换器，需要确定打包策略。

### 下一步

- 增加图片、Markdown 和 TXT 的只读预览。

### 验证

- 已运行 cargo fmt --check。
~~~

## 5. 项目结构与架构边界

默认目录结构如下：

~~~
src/
  components/             # 通用界面组件
  features/library/       # 文件列表、分组、搜索和拖放
  features/preview/       # 各种格式的预览器
  features/settings/      # 应用设置
  lib/                    # 前端类型、状态和工具
src-tauri/
  src/
    commands/             # Tauri command 入口
    filesystem/           # 路径验证、元数据和文件读取
    preview/              # 预览适配器和转换任务
    storage/              # 本地索引、设置和缓存
    windows/              # 托盘、快捷键和 Windows 集成
  capabilities/           # Tauri 权限能力声明
  tauri.conf.json
tests/
  fixtures/               # 无敏感信息的测试文件
docs/
README.md
PROJECT_PROGRESS.md
AGENT.md
~~~

数据调用方向必须保持：

~~~
界面组件
  -> 前端 feature/state
  -> 类型化 Tauri command
  -> Rust 路径校验与业务服务
  -> 文件系统、索引存储或预览适配器
~~~

- 前端负责界面状态、用户交互和内容渲染，不负责绕过 Tauri 权限直接访问任意本地路径。
- Rust 负责路径合法性、文件元数据、持久化、外部转换器进程和任务生命周期。
- 预览器通过统一接口接入，格式差异不能散落在文件列表组件中。
- 文件索引、用户选择状态和预览状态分开管理；预览失败不能破坏文件索引。
- 不在一个组件或 command 中同时实现拖放、索引、解析、转换和界面渲染。
- 长时间运行的读取、解析和转换任务不能阻塞界面线程，必须支持取消或丢弃过期结果。

统一预览接口应至少表达：

~~~
can_preview(path, metadata) -> PreviewSupport
load_preview(path, options) -> PreviewResult
dispose_preview(preview_id)
~~~

具体类型和错误结构以实际代码为准，但新增格式不得绕过统一接口直接嵌入页面。

## 6. 模块交付清单

### 文件库

- 支持文件和文件夹拖放。
- 保存路径、显示名、类型、大小、修改时间和最后预览状态。
- 支持搜索、分组、排序、最近打开和失效路径标记。
- 刷新索引时保留当前选择和滚动位置，不能无故跳回第一项。

### 图片预览

- 支持常见位图格式和透明背景。
- 支持适应窗口、实际尺寸、缩放和旋转。
- 大图使用可释放的对象 URL 或等价机制，关闭预览后及时释放资源。
- 图片缩略图和原图缓存分离，不能为显示缩略图复制整份原图到工作目录。

### 文本与 Markdown 预览

- TXT 以只读文本方式显示。
- 优先识别 UTF-8 BOM、UTF-8；遇到常见中文 Windows 编码时要明确显示编码判断结果，不能静默乱码。
- Markdown 支持渲染视图和原文视图。
- Markdown 生成的 HTML 必须经过 sanitization，禁止脚本、任意 iframe 和危险协议。
- 默认不加载远程图片、远程脚本和远程样式，避免打开本地文档时产生外部网络请求。

### XLSX 预览

- 使用成熟的 XLSX 解析库读取工作簿和工作表。
- 支持切换 Sheet、显示基础表格、空值和常见单元格格式。
- 大表必须限制首屏读取量或使用分页/虚拟滚动，不能无条件把百万行一次性渲染到 DOM。
- 只显示缓存结果和单元格内容，不执行宏、外部链接、嵌入对象或任意公式代码。
- 解析失败、加密工作簿和超过限制的工作簿必须显示明确状态。

### DOCX 预览

- 使用 mammoth、docx-preview 或同等级成熟库进行只读转换。
- 至少覆盖标题、段落、列表、表格和常见图片。
- 转换后的 HTML 必须经过 sanitization，不能执行文档中的脚本或嵌入对象。
- 必须明确说明复杂分页、字体、批注、目录和高级 Word 排版可能与原文不同。

### DOC 预览

- .doc 使用独立的转换适配器，不得假设 DOCX 解析库能够读取它。
- 优先使用 LibreOffice soffice.exe 将文档转换到应用临时目录中的 PDF。
- 转换器路径只能来自应用配置、打包资源或受控的系统探测，不能由前端拼接任意命令。
- 转换进程必须设置超时、捕获退出码并清理临时目录。
- 转换后的 PDF 使用 PDF.js 或同等级成熟组件预览。
- 正式发布前必须明确 LibreOffice 是随安装包提供、由用户安装，还是作为可选依赖；没有转换器时必须显示“无法预览 DOC”的具体原因。
- 不能执行 DOC 宏、外部链接、嵌入对象或文档中的任何可执行内容。

### PDF 预览

- 使用 PDF.js 或同等级成熟库。
- 支持分页、缩放和加载失败提示。
- 禁止把 PDF 内容当作可信 HTML 直接插入页面。

## 7. 命名与代码风格

- TypeScript 变量和函数使用 camelCase，组件和类型使用 PascalCase。
- Rust 模块、函数和变量使用 snake_case，类型使用 PascalCase。
- Tauri command 使用清晰的业务动词，例如 list_files、read_preview_source、convert_doc_to_pdf。
- 预览器使用格式语义命名，例如 MarkdownPreviewer、SpreadsheetPreviewer、DocPreviewAdapter。
- 文件类型判断集中在一个可测试的类型注册表中，禁止在多个组件中散落扩展名判断。
- Rust 不得在用户输入、文件系统和外部进程路径上无条件使用 unwrap 或 expect。
- 用户可见错误不得泄露完整堆栈、内部临时目录、命令行参数、密钥或无关的系统信息。
- 普通源码文件建议不超过 300 行，单个函数建议不超过 60 行。
- 一个文件只承担一个主要职责；重复三次以上的格式判断、错误映射或路径处理应抽取公共实现。

## 8. 前端与 Rust 依赖管理

- Node 依赖通过项目内 npm 管理，提交 package-lock.json。
- Rust 依赖通过 Cargo 管理，提交 Cargo.lock。
- 所有项目运行所需的第三方包必须在项目自己的依赖清单中声明，并安装到项目目录；禁止依赖全局安装、用户目录或项目外未记录的包。新增依赖必须同步更新锁文件，并能通过项目命令复现安装。
- Windows 命令示例优先使用 npm.cmd，避免依赖 PowerShell 对 npm shim 的特殊处理。
- 禁止把全局安装的 Node 包、Rust crate 或转换器当作无记录的运行时依赖。
- 引入第三方库前评估许可证、维护状态、依赖体量、安全记录和 Tauri/WebView2 兼容性。
- 推荐的实现方向：
  - Markdown：成熟 Markdown renderer 加 HTML sanitizer。
  - XLSX：SheetJS 或同等级库。
  - DOCX：Mammoth、docx-preview 或同等级库。
  - PDF：PDF.js 或同等级库。
  - DOC：LibreOffice 转换适配器。
- 不要为了减少依赖而手写 Office、PDF、编码、压缩包或图片解析器。
- 解析库、转换器和前端依赖应锁定版本；升级时记录兼容性和验证结果。

## 9. 本地数据、缓存与文件访问

- 应用设置、文件索引和用户分组存放在 Tauri app data 目录，不得写入项目目录或用户原文档目录。
- 小规模索引可以使用 tauri-plugin-store 或等价的原子 JSON 存储；当查询、标签或数据量明显增长时再评估 SQLite。
- 设置和索引写入必须采用临时文件加原子替换或等价机制，避免应用崩溃产生半写文件。
- 缓存存放在 app cache 目录，必须可清理；缓存损坏不能阻止应用启动。
- 默认存储文件路径和元数据，不保存完整文档内容；确需缓存时必须说明生命周期和清理入口。
- 路径来自用户选择或已保存的用户记录，Rust 端必须再次校验路径存在性、类型和访问权限。
- 读取符号链接、Windows reparse point 或目录外跳转时必须有明确策略，不能因路径拼接造成越界访问。
- 预览任务使用的临时文件必须位于应用临时目录，并在成功、失败、取消和应用退出时尽力清理。
- 对文本、压缩文档、电子表格和图片设定合理的预览大小上限，避免异常文件导致内存耗尽；上限放在统一配置中。
- 不把用户真实路径、文件内容和转换器完整命令行写入普通日志。

## 10. Windows 与 Tauri 运行规则

- Tauri capabilities 遵循最小权限原则，只开放实际使用的窗口、文件、路径、dialog、shell 和 store 能力。
- 前端不能调用任意系统命令；所有外部进程调用必须经过 Rust 白名单适配器。
- LibreOffice 转换器只能接收经过路径校验的输入文件和应用生成的临时输出目录。
- 外部打开文件、打开资源管理器和访问网址必须是明确的用户操作。
- 使用系统托盘、全局快捷键、始终置顶和无边框窗口时，必须在 Windows 11 实机或等价环境验证显示、隐藏、焦点和退出行为。
- 不把 WebView2 的浏览器行为当作完整 Windows 文件预览器；本项目的格式支持以实际适配器和测试结果为准。
- 安装包构建必须明确 WebView2 运行时策略、安装架构和转换器依赖。
- 任何运行时启动的子进程都要处理超时、退出码、标准错误和进程清理。

## 11. 隐私与安全

- 应用默认离线运行，不建立隐藏网络请求，不向第三方发送文件、路径、缩略图或使用统计。
- Markdown、DOCX 和 PDF 的渲染内容必须按不可信输入处理。
- 禁止执行文档宏、脚本、嵌入对象和外部程序。
- 外部 URL 不得在预览区域静默导航；用户点击后才允许调用系统浏览器，并显示目标域名。
- Tauri command 必须验证参数和路径，不能把前端传来的路径直接拼接进命令行。
- 日志必须排除文件内容、访问令牌、私钥和其他敏感配置；调试日志不得默认开启。
- .env、证书、签名私钥和本地导出数据必须加入 .gitignore。
- 预览器发生解析错误时显示通用错误和可诊断状态，不把内部库堆栈直接展示给用户。

## 12. 状态与错误处理

每个文件预览至少区分以下状态：

- idle：尚未开始预览。
- loading：正在读取或解析。
- ready：预览内容可用。
- unsupported：格式暂不支持。
- missing：文件已经移动或删除。
- permission-denied：没有读取权限。
- too-large：超过当前预览限制。
- converter-missing：缺少 DOC 转换器。
- parse-error：文件损坏、加密或解析失败。

- 文件列表切换时，旧预览任务的结果不能覆盖新选择的文件。
- 预览失败不能导致主窗口崩溃或索引丢失。
- 错误状态必须提供可执行的下一步，例如重新选择文件、检查权限、安装转换器或直接打开原文件。
- 空列表、加载中、无搜索结果和失效路径都必须有清晰的界面状态。
- 界面不能用模糊的“发生错误”代替具体的失败原因。

## 13. 单文件体量与重构

- 普通前端、Rust 源码文件建议不超过 300 行。
- 复杂预览器、文件索引服务或转换适配器超过 400 行时必须评估拆分。
- 一个文件只承担一个主要职责。
- 新增格式不应继续扩大文件列表组件；应新增预览器和类型注册。
- 出现重复路径校验、错误映射、临时目录清理或解析限制逻辑时，应抽取可测试的公共模块。
- 重构前记录原因、影响范围和保持不变的外部行为。
- 重构不得顺便改变快捷键、默认文件操作或用户数据存储位置，除非任务明确要求。

## 14. 测试与验证

按照变更范围执行最小必要验证，避免没有风险依据的大量测试。

### 静态和构建检查

适用时执行：

~~~powershell
npm.cmd run build
cargo fmt --check
cargo test
cargo clippy --all-targets --all-features -- -D warnings
npm.cmd run tauri build
~~~

- 只修改文档时不要求运行完整构建。
- 只修改前端预览组件时，优先运行前端构建和对应测试。
- 修改 Rust command、路径访问、转换器或 Tauri capability 时，至少运行 Rust 测试和相应的 Windows 手工验证。
- 只有修改安装配置、Tauri 配置、原生能力或发布流程时，才需要运行完整安装包构建。
- 本地预览和验证使用临时高位端口，避免占用常见端口；验证完成后必须关闭临时服务。
- Windows 浏览器自动化和本地页面验收默认使用已安装的 Microsoft Edge；Playwright CLI 使用 `--browser msedge`，除非用户明确要求，不安装或切换 Chrome。

### 预览手工验证

- 桌面应用和真实文件的手工预览验收由用户自行执行，代理不得代替用户操作或宣称已完成验收；用户明确表示自行验证时，代理应停止桌面/浏览器手工验证，仅进行必要的代码级检查，并避免读取用户真实文件。

使用不含个人信息的测试文件验证：

- 中文文件名、中文目录、空格路径和深层目录。
- 图片缩放、旋转、透明背景和大尺寸图片。
- UTF-8 Markdown、包含代码块和表格的 Markdown、普通 TXT。
- 多 Sheet、空单元格、日期和基础格式的 XLSX。
- 包含标题、列表、表格和图片的 DOCX。
- 正常 DOC、损坏 DOC、缺少 LibreOffice 时的 DOC。
- 多页 PDF、损坏 PDF 和超大文件。
- 文件被移动、删除、拒绝访问、重复拖入和应用重启后的索引恢复。
- 预览失败、取消、快速切换文件时界面不会显示旧文件内容。

测试样本统一放在 tests/fixtures/，不得使用用户真实资料。涉及 .doc 的测试必须确认转换器进程退出并清理临时文件。

如果某项验证无法执行，必须在 PROJECT_PROGRESS.md 和最终说明中记录原因与影响范围。

## 15. 构建与发布

- 发布前必须确认前端依赖、Cargo 依赖和 Tauri 版本均来自锁定文件。
- Windows 安装包只包含经过确认的应用资源和必要运行时，不包含用户测试文件、日志、缓存和本地配置。
- 发布构建必须检查应用版本、安装包架构、图标、托盘行为、卸载行为和首次启动。
- .doc 支持的发布说明必须明确 LibreOffice 的提供方式和版本要求。
- 签名证书、私钥和签名密码不得进入仓库、日志或安装包构建产物。
- 构建失败、转换器缺失或某格式未通过验证时，不得把安装包标记为完整版本。
- 未经用户明确要求，不执行发布、上传、创建 GitHub Release 或推送远程仓库。

## 16. 文档同步

以下变更必须同步更新 README、PROJECT_PROGRESS.md 或相关 docs：

- 项目启动方式、目录结构和技术基线变化。
- 支持或移除文件格式。
- 预览库、转换器和外部运行时依赖变化。
- 文件索引、缓存、设置和本地数据位置变化。
- 快捷键、托盘、窗口行为和默认文件操作变化。
- Tauri capability、隐私策略或安全边界变化。
- 安装包、WebView2、LibreOffice 和 Windows 支持范围变化。
- 测试命令、构建命令和已知限制变化。

文档必须区分“已实现”“部分支持”“依赖外部组件”和“计划实现”，不能把设计草案写成当前能力。

## 17. Definition of Done

一个功能完成必须满足：

- 代码符合本文件规定的目录边界、命名和依赖管理方式。
- 用户路径、文件权限、异常文件和临时资源有明确处理。
- 新增预览格式已接入统一预览接口，并有成功和失败状态。
- HTML、宏、脚本、外部链接和外部进程边界已按功能影响范围处理。
- 相关的最小必要测试、构建或 Windows 手工验证已完成。
- README、PROJECT_PROGRESS.md 和相关格式说明已同步。
- .gitignore 没有遗漏构建产物、缓存、测试私有数据和敏感配置。
- 不存在未记录的系统依赖、未验证的“支持”声明或隐藏网络行为。
- 功能可运行、可诊断、可清理；临时方案和已知限制已明确记录。
