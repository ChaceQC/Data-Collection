# 本地资料工作台 0.3.x 悬浮球悬停弹窗优化实施计划

> 计划类型：替换旧版 `PROJECT_PLAN.md` 的新执行计划
> 计划目标：优化悬浮球在鼠标覆盖/靠近时的弹窗显示，重点解决悬浮球位于屏幕四边、四角、任务栏附近和多显示器边缘时的展开、命中和收起问题
> 计划版本线：按用户要求，每个阶段使用 `0.3.x` 版本；阶段版本依次为 `0.3.1` 至 `0.3.6`
> 当前仓库基线：规划前应用版本为 `0.3.0`，`dev` 与 `main` 指向同一发布提交
> 验收方式：Windows 11 Tauri 桌面端由用户手动验收；浏览器页面、自动化测试和 CI 构建不能替代用户验收
> 当前状态：阶段 A-F 的代码实现、自动验证、Windows 11 实机验收和 `v0.3.6` Release 已完成

## 1. 计划替换与目标

### 1.1 旧计划处理

- 删除旧版 `PROJECT_PLAN.md` 中关于 `0.3.0` 系统托盘、关闭生命周期、主窗口拖动和 `WebView2Loader.dll` 发布优化的执行内容。
- 由本文件作为当前唯一的项目执行入口，后续代理不得继续把旧版阶段 A-H 当作本轮待办。
- 已完成的历史事实仍以 `PROJECT_PROGRESS.md` 和 Git 历史为准；替换计划不删除进度历史，不把旧功能重新标记为未完成。
- 根目录没有另行维护的 `PLAN.md`；后续如新增计划文件，必须先同步本文件的唯一入口约定。

### 1.2 本轮真正要解决的问题

现有悬浮球已经具备独立 Tauri 窗口、文件/文件夹拖入、最近五条记录、靠近展开、自由移动、四边贴边、位置恢复和收藏同步能力。本轮不重新实现这些基础能力，而是围绕下面的用户体验缺陷做针对性优化：

1. 鼠标覆盖悬浮球后，弹窗应稳定、可预测地打开，不能因为靠近检测、DOM 指针事件和窗口移动之间的时序差异而不打开、重复打开或来回闪烁。
2. 鼠标从悬浮球移动到最近记录弹窗时，球体和弹窗之间不能出现会触发收起的不可交互空隙。
3. 悬浮球在左、右、上、下边缘以及四个角落时，弹窗必须朝工作区内部展开，球体视觉位置保持不跳动，弹窗不能被屏幕边界或任务栏截断。
4. 当首选展开方向空间不足时，系统必须有明确的备选方向、尺寸压缩或滚动策略，不能把窗口放到负坐标、另一块屏幕或不可见区域。
5. 拖动悬浮球、拖入资料、点击最近记录、收藏、键盘操作和显式收起之间不能互相误触发。
6. 高 DPI、负坐标显示器、不同缩放比例、显示器断开重连和应用重启后，悬浮球与弹窗仍应保持可见且可用。

### 1.3 总体成功标准

完成阶段 F 后，用户在 Windows 11 桌面端手动验收应确认：

- 鼠标进入悬浮球有效命中区域后，弹窗只打开一次；打开延迟符合阶段 A 约定，连续移动鼠标不会导致重复 resize、重复定位或闪烁。
- 鼠标从球体移动到弹窗内容、弹窗操作按钮或关闭按钮时，弹窗保持打开；鼠标离开完整交互区域后按延迟规则收起。
- 左、右、上、下四种贴边位置以及四角位置都向工作区内展开，最近记录面板、球体、关闭按钮和阴影的可用区域不被工作区外边界截断。
- 面板内容过多时只在面板内部滚动；反馈文案、收藏按钮、文件名、省略号和关闭按钮不会相互覆盖，也不会导致窗口几何不断抖动。
- 轻微移动悬浮球时不会打开面板；开始拖动后面板立即停止展开/收起计时，拖动结束后位置保存、贴边和再次展开方向一致。
- 快速覆盖/离开、覆盖后拖动、覆盖后点击、打开过程中离开、打开后切换显示器和连续收藏等组合操作不会留下旧定时器、旧异步结果或失去焦点的窗口。
- 原有最近记录、收藏、路径失效提示、文件打开、位置恢复、设置开关、托盘生命周期和浏览器安全回退不回归。
- 仅用户明确回复手动验收通过后，才允许执行本计划第 9 节的 commit、合并、推送和 Release 流程。

## 2. 当前真实基线

### 2.1 已存在的模块和行为

- 前端原型位于 `prototype/`，基于 React 19、Vite 6 和 Tauri 2；界面和代码注释默认使用中文。
- 悬浮球入口位于 `prototype/src/features/floating-ball/`：
  - `FloatingBallWindow.jsx` 负责窗口内状态、Tauri 事件、拖动、记录、打开/收起和窗口尺寸/位置调整。
  - `FloatingBallPanel.jsx` 负责最近记录面板、文件打开和收藏按钮。
  - `floatingBallModel.js` 负责状态常量、最近记录、靠近判断、展开方向、窗口几何、贴边和 DPI 坐标转换。
  - `floatingBallApi.js` 负责 Tauri command、窗口位置/尺寸、当前显示器和拖动 API 封装。
- 原生窗口与显示器逻辑位于：
  - `prototype/src-tauri/src/windows/floating_ball.rs`：创建透明、置顶、无装饰、跳过任务栏的 `floating-ball` 窗口。
  - `prototype/src-tauri/src/windows/mod.rs`：创建、显示、隐藏、销毁悬浮球和重启靠近监视。
  - `prototype/src-tauri/src/windows/monitor.rs`：工作区、显示器、位置、DPI、光标靠近检测和边缘位置计算。
  - `prototype/src-tauri/src/storage/floating_ball.rs`：独立的 `floating-ball.json` v1 位置持久化。
  - `prototype/src-tauri/src/commands/floating_ball.rs`：最近记录、位置读写、主窗口打开和悬浮球重试 command。
- 当前前端常量为球体 `64 DIP`、面板约 `320 x 322 DIP`、进入靠近阈值 `28 DIP`、离开阈值 `16 DIP`、打开延迟 `120 ms`、收起延迟 `250 ms`、贴边阈值 `24 DIP`。
- 当前原生靠近监视约每 `80 ms` 轮询一次光标，向 `floating-ball` 窗口发送 `floating-near`；前端还监听窗口指针进入/离开、拖放和窗口移动事件。
- 当前浏览器回退使用演示数据和 HTML 拖放，不调用真实文件路径、Tauri 窗口 API、托盘或本地文件系统。
- 当前悬浮球 capability 已开放最小的窗口位置/尺寸、当前显示器、缩放因子、拖动、事件监听和既有悬浮球 command；新增权限必须有明确的功能理由。

### 2.2 本轮重点检查的现有热点

以下是需要在实现阶段验证和收敛的风险点，不在本次计划制定时直接断言每一项都是已确认缺陷：

- `getPanelDirection` 对贴边模式优先使用固定的内向方向，但首选方向是否有足够的面板空间需要和工作区尺寸一起重新验证。
- `getExpandedWindowGeometry` 在面板尺寸大于可用工作区、球体靠近角落或坐标为负时，需要确保 clamp 不产生工作区外的根窗口位置。
- 当前打开/收起流程需要连续调用 `setSize` 与 `setPosition`；窗口调整期间若旧异步结果晚到，可能覆盖新方向、新显示器或拖动状态。
- 当前 `FloatingBallWindow.jsx` 承担的职责较多，已超过普通源码建议体量；本轮如继续增加状态分支，必须按职责拆分，不能把所有竞态继续堆进同一组件。
- 当前根容器使用 `pointer-events: none`，球体和面板单独恢复交互；需要确认窗口扩展后面板、球体、关闭按钮和过渡区域的有效命中范围连续。
- 当前工作区和窗口坐标在前端存在物理像素与 DIP 的转换；多显示器不同缩放比例下必须统一计算域，不能依赖单显示器的正坐标假设。

## 3. 产品行为契约

### 3.1 几何术语和坐标约定

实现时统一使用以下术语，避免把球体位置、透明窗口根位置和弹窗位置混为一谈：

| 名称 | 含义 | 约束 |
| --- | --- | --- |
| `ballRect` | 球体在屏幕工作区中的可见矩形 | 打开前后视觉左上角和尺寸保持稳定 |
| `panelRect` | 最近记录面板的实际矩形 | 必须优先位于工作区内，面板内部可滚动 |
| `hostRect` | Tauri 透明窗口的根矩形 | 包含球体和面板的交互区域，不得落到不可见坐标 |
| `workArea` | 当前显示器排除任务栏后的可用工作区 | 作为边界，不使用整块虚拟桌面代替 |
| `direction` | `left` 或 `right` | 表示面板相对于球体的展开方向；顶部/底部位置不切换为垂直弹窗 |
| `scaleFactor` | 当前显示器或悬浮球窗口的有效 DPI 缩放 | 物理像素调用与 DIP 布局之间只能经过一个明确转换层 |

具体实现优先在纯模型中以 DIP 计算布局，再在调用 Tauri `setSize`/`setPosition` 前统一转换为物理像素；如果验证发现 Tauri 当前 API 对虚拟桌面坐标的语义要求全程使用物理像素，则应改为全程物理像素计算，并保留 CSS 尺寸换算，不能在同一函数中混用两套坐标。

### 3.2 悬停状态机

实现必须具备可追踪的状态转移，不能只依赖多个互相独立的 boolean 和 timer：

```text
collapsed
  -> hover-pending       鼠标进入球体或有效靠近区域
  -> opening             打开计时结束，开始计算几何和调整窗口
  -> open                面板已完成渲染，球体与面板都可交互
  -> close-pending       鼠标离开完整交互区域，等待收起延迟
  -> closing             开始还原球体窗口
  -> collapsed

hover-pending/open/close-pending/opening
  -> dragging            指针移动超过拖动阈值
dragging
  -> collapsed            拖动结束并保存位置
open/recording
  -> error 或 open         操作失败或完成后保留可诊断状态
```

必须满足以下不变量：

- 同一时刻最多有一个打开计时器、一个收起计时器和一个有效的窗口几何任务。
- 每次打开、收起、拖动或窗口销毁都生成新的操作序号；旧的异步 `setSize`、`setPosition` 或查询结果不能覆盖新状态。
- `open` 状态的交互区域至少包含球体、面板、面板操作按钮和连接过渡区域；只要指针仍在该区域内，不能因为 `floating-near=false` 立即收起。
- 开始拖动后取消打开/收起计时器，关闭打开中的面板几何任务；拖动过程不自动重新打开面板。
- 显式点击、Enter/Space、Escape 和关闭按钮的语义独立于靠近轮询；显式收起后，直到鼠标重新离开并再次进入，不得立即被旧的 near 事件打开。
- 记录资料、刷新最近列表、收藏和打开主窗口失败时，面板仍可收起和再次打开，错误状态不能锁死悬浮球。

### 3.3 鼠标覆盖和收起规则

- “鼠标覆盖”是主要触发方式：指针进入球体可见命中区域后进入 `hover-pending`，在约定延迟后打开面板。
- 现有 `floating-near` 受控轮询作为桌面端补充触发源时，必须和指针事件汇聚到同一个状态机，不能各自维护一套打开逻辑。
- 目标打开延迟为 `100-180 ms`，默认优先保留现有 `120 ms`；目标收起延迟为 `250-500 ms`，默认优先保留现有 `250 ms`。如实机体验需要调整，必须在阶段进度中记录调整理由。
- 鼠标从球体移动到面板时，允许经过一个短暂连接区域；连接区域不能覆盖主窗口或拦截无关窗口输入，也不能在面板外形成大块透明点击层。
- 鼠标离开球体但进入面板时，不触发收起；鼠标离开球体和面板的完整区域后才进入 `close-pending`。
- 面板打开失败、当前窗口不可用、显示器信息读取失败或窗口调整失败时，恢复到可见的 `64 x 64 DIP` 球体，并显示不泄露路径和内部堆栈的错误反馈。

### 3.4 四边和四角展开规则

| 球体位置 | 方向判断 | 视觉要求 |
| --- | --- | --- |
| 左侧区域/左边缘 | 向右 | 面板位于球体右侧并留在当前工作区内，球体视觉位置不跳 |
| 右侧区域/右边缘 | 向左 | 面板位于球体左侧并留在当前工作区内，球体视觉位置不跳 |
| 上边缘或下边缘 | 仍按球体的水平位置选择左/右 | 面板只在垂直轴做工作区内 clamp，不切换成上/下布局 |
| 四角 | 按水平位置选择左/右 | 面板不跨到另一块显示器、不遮住球体，关闭按钮保持可见 |
| 自由位置 | 先比较左右剩余空间 | 一侧不足时选择另一侧；两侧都不足时选空间较大的一侧并压缩面板/保留内部滚动 |

方向选择和几何计算必须考虑：面板宽高、球体尺寸、工作区边界、任务栏、DPI、负坐标和面板最小可用尺寸。若当前工作区小于默认面板尺寸，不能简单把根窗口 clamp 到负范围；应优先缩小面板宽/高的可视区域并保证列表滚动，或选取可用空间最大的方向，并在界面保持可操作。

### 3.5 交互、可访问性和内容边界

- 最近记录、收藏、打开主窗口和错误反馈继续复用现有索引 ID、Tauri command 和 `index-changed` 同步，不新增第二套文件数据。
- 文件名继续使用省略号和安全文本节点显示；不能把完整路径、文件内容、密钥或内部命令行写入面板、事件 payload 或普通日志。
- 面板保持固定的最大布局边界，列表在面板内部滚动；空列表、加载中、成功、部分成功、失败、失效路径和收藏处理中都必须有稳定布局。
- 收藏按钮、关闭按钮、最近记录行和球体均保留清晰的 `aria-label`、`aria-expanded`、`aria-pressed`、键盘焦点和 `title`；操作按钮不能被窗口拖动逻辑吞掉。
- 面板打开后，焦点策略必须明确：默认不抢夺用户正在使用的主窗口焦点；键盘从球体进入面板时不能形成焦点陷阱；Escape 可收起并把焦点返回球体。
- 浏览器回退继续只模拟内存状态，不能为验证悬浮球而绕过 Tauri 权限读取真实本地路径。

## 4. 技术架构与变更边界

### 4.1 推荐的职责拆分

| 模块 | 本轮责任 | 约束 |
| --- | --- | --- |
| `floatingBallModel.js` | 纯状态转移、方向选择、矩形计算、边界和坐标转换 | 无 Tauri 调用；新增函数可直接用 Node 测试 |
| `floatingBallHoverController.js` 或等价模块 | 合并 pointer/near 事件、timer、操作序号和状态机 | 不负责索引写入和原生路径访问 |
| `FloatingBallWindow.jsx` | 组合状态、渲染球体/面板、调用受控窗口 API | 控制器和几何逻辑抽出后，继续评估 300 行体量边界 |
| `FloatingBallPanel.jsx` | 最近列表、反馈和按钮交互 | 不负责窗口位置、尺寸或显示器判断 |
| `floatingBallApi.js` | 位置、尺寸、监听、拖动和既有 command 封装 | 新增 API 前先确认现有 Tauri API 能否复用 |
| `styles.css` | 球体、面板、连接区域、滚动、焦点和方向样式 | 不通过大面积透明元素劫持页面外输入 |
| `windows/monitor.rs` | 工作区、显示器、DPI 和原生边界计算 | 保持无路径、无密钥日志；不改变索引语义 |
| `windows/mod.rs` / `windows/floating_ball.rs` | 窗口生命周期和调整顺序 | 只在前端无法解决原生窗口问题时修改 |
| `storage/floating_ball.rs` | 位置格式兼容和安全回退 | 优先保持 `floating-ball.json` v1，若升级必须写迁移和回退 |
| `commands/floating_ball.rs` | 既有 command 的错误映射和状态一致性 | 不把任意路径、窗口参数或命令行交给前端 |
| capability 文件 | 仅登记实际新增的 Tauri 权限 | 最小权限；禁止为方便调试开放任意 shell/文件系统权限 |

### 4.2 受保护边界

本轮不应顺手修改与目标无关的功能或托管约定：

- 不修改 `.openai/hosting.json`、`worker/index.js`、`scripts/prepare-sites-build.mjs` 和 `tests/sites-worker.test.mjs`。
- 不重写文件索引 v3、设置 v2、预览协议、托盘菜单、主窗口拖动规则或原文件操作语义，除非悬浮球竞态确实要求共享状态修复，并在进度中说明影响。
- 不新增云同步、账号、后台服务、开机启动、全局鼠标钩子、全局快捷键、遥测、全文检索、标签/分组或文件物理复制移动删除功能。
- 不引入新的 UI 框架或全局依赖；确需新增依赖时，必须先评估许可证、体量、维护状态、Tauri/WebView2 兼容性并更新锁文件。
- 不使用用户真实资料、真实路径、真实密钥或包含个人信息的日志作为测试输入。

## 5. 分阶段实施清单

每个阶段的执行节奏固定为：实现本阶段代码 -> 执行本阶段最小必要的代码级验证 -> 统一版本入口 -> 生成可供用户手动验收的桌面版本 -> 等待用户明确反馈 -> 记录结果后进入下一阶段。本次在同一工作区连续完成阶段 A-E 的代码级交付，未把未执行的 Windows 手工验收写成通过，也未提前进入发布流程。

### 阶段 A：悬停与窗口几何契约（`0.3.1`）

**目标：**先把“球体位置、面板位置、根窗口位置、工作区、方向和 DPI”定义成可测试的纯模型，为后续消除边缘问题建立唯一事实来源。

**实现清单：**

- [x] 复核 `FloatingBallWindow.jsx`、`floatingBallModel.js`、`monitor.rs`、`floating_ball.rs` 和 `styles.css` 的实际窗口尺寸、坐标、事件和样式关系。
- [x] 在 `floatingBallModel.js` 中定义或收敛 `ballRect`、`panelRect`、`hostRect`、`workArea`、`direction` 和 `scaleFactor` 的数据结构。
- [x] 将方向选择、面板矩形、根窗口矩形、球体锚点保持和工作区 clamp 收敛到纯函数；调用方不再自行拼接另一套边缘计算。
- [x] 明确物理像素与 DIP 的转换入口，禁止一个布局函数既把物理坐标当 DIP 又把 DIP 尺寸直接传给 Tauri。
- [x] 定义状态机事件名称和操作序号字段；本阶段可以只接入模型，不要求一次完成所有 UI 状态。
- [x] 评估默认面板尺寸、最小可用宽高、内部列表滚动高度和边缘安全间距；把常量集中保存，避免 CSS、JS、Rust 各自维护不同数字。
- [x] 保持索引 v3、位置文件 v1 和既有 Tauri command 兼容，不在本阶段改变持久化字段。
- [x] 在实现候选中统一检查版本入口；当前候选版本为 `0.3.6`，阶段 A-E 未分别发布中间版本。

**最小代码级验证：**

- [x] 扩充 `prototype/tests/floating-ball-model.test.mjs`，覆盖四边、四角、自由位置、工作区不足、负坐标和至少两种 scale factor。
- [x] 验证球体锚点在展开前后相对屏幕坐标不变，面板矩形不越过合法工作区；若空间不足，验证降级结果仍可操作。
- [x] 检查 `git diff --check`，不执行与本阶段无关的大规模测试。

**用户手动验收点：**

- [x] 用户在 Windows 11 Tauri 开发版确认球体仍可显示、移动、拖入文件并保持最近记录。
- [x] 用户观察左/右/上/下边缘和四角的面板目标方向，确认阶段 A 的几何基线没有明显反向或跳位。

**阶段完成条件：**模型测试通过，版本入口一致，用户明确回复阶段 A 手动验收结果；失败项必须记录具体显示器、边缘、DPI 和复现步骤。

### 阶段 B：悬停状态机与异步生命周期（`0.3.2`）

**目标：**让“覆盖球体 -> 延迟打开 -> 进入面板 -> 延迟收起”成为单一、可取消、不会被旧异步结果覆盖的交互流程。

**实现清单：**

- [x] 将 pointer enter/leave、`floating-near` 和显式点击统一交给同一个 hover controller 或等价状态机。
- [x] 实现 `hover-pending`、`opening`、`open`、`close-pending`、`closing` 和 `dragging` 的明确转移，移除无主 timer 和互相覆盖的分支。
- [x] 保留或校准 `120 ms` 打开延迟和 `250 ms` 收起延迟；打开/收起定时器只能各有一个，重新进入时必须取消收起定时器。
- [x] 为每次打开、收起、拖动和窗口重建分配操作序号；窗口位置、显示器、缩放和尺寸查询返回后先检查序号、当前状态和窗口可用性。
- [x] 将 resize/move 调整串行化，保证旧方向的迟到结果不会恢复旧尺寸；窗口调整失败时恢复球体并显示可诊断反馈。
- [x] 保证鼠标从球体进入面板时完整交互区域连续；面板按钮、收藏和关闭按钮的 pointer 事件不能触发根容器误收起。
- [x] 保证显式 Escape/关闭按钮收起后不会被旧 `floating-near=true` 立即重新打开；重新进入球体后才重新进入 hover 流程。
- [x] 开始拖动、记录资料、关闭悬浮窗、重建窗口和应用退出时清理 timer、监听和未完成操作。
- [x] 在实现候选中统一检查版本入口，并在 `PROJECT_PROGRESS.md` 增加真实的实现、验证和用户验收状态。

**最小代码级验证：**

- [x] 为 hover controller/model 增加进入、离开、重入、快速切换、打开失败、收起失败、显式收起和拖动抢占测试。
- [x] 运行 `npm.cmd run test:floating-ball`；如修改 Rust 状态边界，再运行对应的 `cargo test`，不默认运行全项目测试。
- [x] 检查浏览器回退不会调用 Tauri API，且没有未处理 Promise 或重复监听。

**用户手动验收点：**

- [x] 用户将鼠标覆盖球体，确认弹窗打开一次且延迟稳定。
- [x] 用户从球体连续移动到面板、文件行、收藏按钮和关闭按钮，确认面板不闪退、不收起、不抢错焦点。
- [x] 用户快速进入/离开多次，确认没有重复弹窗、抖动、卡住或关闭按钮失效。
- [x] 用户在面板打开时拖动球体，确认拖动优先级正确，未误打开文件或收藏。

**阶段完成条件：**状态转移测试通过，打开/收起竞态没有未处理错误，用户明确回复阶段 B 手动验收通过或列出待修复项。

### 阶段 C：四边、四角和工作区边界展开（`0.3.3`）

**目标：**解决悬浮球位于屏幕边缘时最核心的弹窗可见性、方向和锚点问题。

**实现清单：**

- [x] 以当前显示器 `workArea` 为边界，不使用包含任务栏的整屏尺寸；确认左侧、上侧和负坐标显示器的工作区起点处理正确。
- [x] 对左、右、上、下贴边分别计算内向方向，并验证面板与球体的相对矩形不反向。
- [x] 对四角位置定义另一轴的对齐策略：优先保持球体锚点，必要时沿工作区内移动面板，但不能遮住球体或把关闭按钮移出可见区域。
- [x] 在首选方向空间不足时按固定优先级选择可完整容纳的备选方向；仍不足时压缩面板可视宽高并让列表滚动，不能让根窗口坐标小于工作区起点或大于工作区末端。
- [x] 将最小面板宽高、边缘间距、球体尺寸和工作区边界纳入同一份几何配置；CSS 的实际布局尺寸必须与模型输入一致。
- [x] 明确原生窗口调整顺序，避免先 resize 导致窗口短暂跨屏或面板闪到另一侧；每次调整后验证窗口仍在预期显示器和工作区内。
- [x] 保持面板阴影是视觉效果，不把阴影越过边界误判为布局矩形；命中区域以实际可交互矩形为准。
- [x] 在实现候选中统一检查版本入口，并同步进度中的文件、验证和用户验收结果。

**最小代码级验证：**

- [x] 在 `floating-ball-model.test.mjs` 覆盖左上、右上、左下、右下、四条边的标准尺寸和面板不足尺寸。
- [x] 覆盖工作区起点为负数、显示器横向排列、不同 scale factor、任务栏改变工作区高度和 `ballRect` 贴边的情况。
- [x] 覆盖首选方向不可用时的备选方向、面板内部滚动尺寸和锚点保持。
- [x] 本轮未修改 `monitor.rs`，不新增原生边界代码；Rust 版本检查结果另记于进度。

**用户手动验收点：**

- [x] 用户依次把球体拖到左、右、上、下四条边，分别覆盖球体，确认面板朝屏幕内部展开且没有被截断。
- [x] 用户把球体拖到四个角落，确认面板不跨到另一块显示器、不压住球体、不出现空白大偏移。
- [x] 用户在面板内容达到五条、出现反馈文案和出现失效路径时检查面板底部、滚动条、关闭按钮和收藏按钮。
- [x] 用户确认任务栏位置不影响面板的可见性和操作。

**阶段完成条件：**所有标准边缘和角落模型用例通过，Windows 11 四边四角手动验收明确通过；任一边缘截断、跳屏或无法点击都阻断阶段完成。

### 阶段 D：面板视觉、命中区域和可访问性（`0.3.4`）

**目标：**在几何正确的基础上，把面板做成稳定、紧凑、可读、可操作的悬浮工具面板。

**实现清单：**

- [x] 将 `FloatingBallWindow.jsx` 中的状态控制、事件控制和渲染职责拆开，避免继续扩大单文件；普通源码文件和函数遵守 `AGENT.md` 的体量建议。
- [x] 让面板拥有稳定的最大宽高和内部滚动区域；反馈条出现或消失不能让根窗口在鼠标下连续改变尺寸。
- [x] 为四个方向提供一致的面板排列和关闭按钮定位；关闭按钮不能遮住标题、文件行、收藏按钮或面板边界。
- [x] 保证 `.floating-ball-window`、球体、面板和关闭按钮的 `pointer-events` 层级清晰；不使用不可见的大面积元素拦截主窗口或其他应用输入。
- [x] 优化长文件名、中文文件名、空列表、加载中、部分成功、错误和失效路径状态，保证文字在父容器内省略或换行，不改变布局宽度。
- [x] 保留现有图标库和按钮语义；操作按钮使用图标和可访问名称，焦点环、禁用态、收藏态和错误态清晰可见。
- [x] 检查高 DPI 下字体、边框、阴影、球体和面板没有重叠；浏览器回退的面板尺寸也保持可用。
- [x] 在实现候选中统一检查版本入口，并同步进度和已知限制；没有把视觉优化描述成已完成的 Windows 验收。

**最小代码级验证：**

- [x] 运行 `npm.cmd run test:floating-ball` 和 `npm.cmd run build`，确认 Sites 产物仍能生成。
- [x] 对涉及的 DOM 状态增加必要的静态检查或模型断言，重点检查 `aria-expanded`、`aria-pressed`、关闭按钮和事件清理。
- [x] 本轮未新增依赖，未运行与面板优化无关的依赖升级。

**用户手动验收点：**

- [x] 用户检查最近记录为空、五条记录、长中文名称、失效记录和记录反馈时，确认面板没有溢出、跳动或文字遮挡。
- [x] 用户点击最近记录、收藏、关闭按钮并使用 Escape，确认每个动作只执行一次，鼠标仍在面板内时不会误收起。
- [x] 用户使用键盘聚焦球体、打开面板、移动到按钮和收起面板，确认焦点可见、名称正确、没有焦点陷阱。

**阶段完成条件：**面板视觉和交互在桌面版及浏览器回退均可用，用户明确回复阶段 D 手动验收结果；发现布局溢出、按钮不可点击或焦点丢失时不得进入阶段 E。

### 阶段 E：拖动、多显示器、DPI 与回归稳定性（`0.3.5`）

**目标：**把悬停弹窗优化与现有拖动、贴边、位置恢复、最近记录和窗口生命周期组合起来验证。

**实现清单：**

- [x] 验证指针按下、拖动阈值、开始原生拖动、结束拖动和位置保存之间的优先级；轻微抖动不能触发展开，真实拖动不能打开面板。
- [x] 拖动结束后重新计算当前显示器、工作区和贴边状态；位置保存继续使用 `floating-ball.json` v1 的安全格式和原子写入。
- [x] 已将多显示器、DPI、负坐标和任务栏边界统一交给工作区几何模型；真实跨屏行为已由用户在 Windows 11 手工验收通过。
- [x] 已保留现有位置损坏/工作区回退路径并避免清空索引；显示器断开等桌面异常已由用户手工验收通过。
- [x] 验证 `showFloatingWindow` 设置关闭/开启、托盘显示/隐藏、显式退出、主窗口关闭隐藏和悬浮球重建时，靠近监视、事件监听和面板状态正确清理。
- [x] 验证 `index-changed`、最近记录刷新、收藏、拖入记录和打开主窗口不会让打开中的面板回到错误方向或覆盖新数据。
- [x] 保留现有低频靠近监视、滞回、延迟和几何任务串行化，未用高频循环解决视觉问题。
- [x] 在实现候选中统一检查版本入口，并同步 `PROJECT_PROGRESS.md` 的进行中、风险、下一步和验证结果。

**最小代码级验证：**

- [x] 运行 `npm.cmd run test:floating-ball`，覆盖拖动抢占 hover、位置保存和重建后的状态复位。
- [x] 运行 `cargo fmt --check`、相关 `cargo test` 和 `cargo check`；本轮未新增 capability、Tauri 配置权限或发布资源。
- [x] 运行 `npm.cmd run build`，确认 `.openai/hosting.json`、Sites worker 和浏览器回退不受影响。

**用户手动验收点：**

- [x] 用户在单显示器、双显示器、不同缩放比例和至少一块负坐标显示器上反复拖动球体并覆盖打开面板。
- [x] 用户验证四边贴边、自由位置、再次启动位置恢复、显示器断开/恢复后的安全位置和任务栏附近行为。
- [x] 用户在面板打开、记录资料、收藏、刷新列表和切换设置时进行快速操作，确认没有闪烁、死锁、重复监听或卡死。
- [x] 用户回归确认主窗口、托盘、预览、索引、收藏、文件打开和浏览器回退未受影响。

**阶段完成条件：**跨显示器/DPI/拖动/生命周期组合手动验收通过，自动检查只作为辅助证据；用户已确认本阶段全部场景通过。

### 阶段 F：发布候选、最终手动验收与 `0.3.6` 发布（`0.3.6`）

**目标：**冻结悬浮球优化范围，完成最终版本同步和用户手动验收；只有用户明确确认通过后，才执行 Git 合并、推送和自动 Release。

**发布前清单：**

- [x] 汇总阶段 A-E 的实际代码、测试、手工结果和未解决风险，不把计划勾选当作实现证据。
- [x] 确认本轮只修改悬浮球相关代码、必要测试、必要 capability/配置和同步文档；删除临时日志、截图索引、构建产物和真实资料。
- [x] 将所有版本入口统一更新为 `0.3.6`，至少包含 `prototype/package.json`、`prototype/package-lock.json` 根包、`prototype/src-tauri/tauri.conf.json`、`prototype/src-tauri/Cargo.toml`、`prototype/src-tauri/Cargo.lock` 根包、`README.md`、`prototype/README.md`、`PROJECT_PLAN.md` 和 `PROJECT_PROGRESS.md`。
- [x] 将 `PROJECT_PROGRESS.md` 更新为真实的验收通过和发布准备状态，明确自动验证、用户手工验收和本轮阶段历史。
- [x] 运行发布候选所需的最小检查：`git diff --check`、`npm.cmd run test:floating-ball`、`npm.cmd run build`、`cargo fmt --check`、`cargo test`、`cargo check`；本轮未修改 Rust 原生逻辑或 capability，未追加 `cargo clippy`，但已执行 `npm.cmd run tauri:build` 并通过 loader 校验。
- [x] 已生成供用户验收的 Windows x64 Tauri `0.3.6` Release 候选包；代理不得用浏览器演示代替真实桌面验收。

**用户 Windows 11 手动验收清单：**

1. 启动 `0.3.6` 候选版本，确认只出现一个悬浮球窗口，球体可见、置顶、不会出现在任务栏。
2. 在左、右、上、下四条边分别覆盖球体，确认面板按内向方向打开，球体不跳动，面板没有被工作区边缘截断。
3. 在左上、右上、左下、右下四个角落覆盖球体，确认面板不跨屏、不出现在任务栏外侧，关闭按钮和最近记录可点击。
4. 鼠标从球体移动到面板标题、文件行、收藏按钮和关闭按钮，确认整个过渡过程不闪烁、不提前收起。
5. 鼠标离开完整交互区域，确认面板按延迟收起；重新进入时不会出现双面板或旧位置覆盖。
6. 快速覆盖/离开至少十次，确认没有重复 resize、位置抖动、窗口残留或 CPU 持续升高的明显异常。
7. 面板分别处于空列表、五条记录、长中文文件名、失效路径、成功反馈、部分成功反馈和错误反馈时，确认文字、滚动、按钮和关闭提示不重叠。
8. 点击最近记录、收藏/取消收藏、关闭按钮，使用 Enter、Space 和 Escape，确认每个动作只执行一次且焦点行为合理。
9. 从资源管理器拖入文件和文件夹，确认拖入提示、记录过程和完成反馈不会错误打开/收起面板，最近五条和主窗口同步正确。
10. 鼠标按下球体后只做轻微移动，确认不会误启动拖动或弹窗；真实拖动球体，确认拖动优先且结束后位置保存。
11. 把球体拖到四边和四角，关闭并重新打开应用，确认位置、方向和弹窗展开行为恢复一致。
12. 在双显示器、不同 DPI、负坐标和任务栏位于不同边的环境中重复第 2、3、10、11 项。
13. 临时断开或改变显示器布局后重新启动，确认悬浮球回到当前可见安全位置，索引和收藏不丢失。
14. 关闭/开启“显示悬浮窗”，从托盘显示/隐藏悬浮窗，确认靠近监视和面板监听不会重复或残留。
15. 主窗口关闭隐藏、托盘打开主窗口和显式退出后，确认悬浮球生命周期与已有约定一致。
16. 回归主窗口索引、预览、收藏、文件打开、资源管理器定位和浏览器回退，确认没有因本轮状态机变化而回归。
17. 检查安装/候选包版本、Windows x64 架构和 `WebView2Loader.dll`，确认 `0.3.6` 是从 `0.3.0` 正常递增的版本。
18. 用户在 `PROJECT_PROGRESS.md` 中记录 Windows 版本、WebView2 版本、显示器/DPI、测试夹具、通过项、失败项和最终结论。

**阶段 F 完成条件：**

- [x] 用户已于 `2026-08-30` 明确回复上述 Windows 11 手动验收全部通过。
- [x] 所有 P0/P1 问题清零；用户确认没有阻断发布的 P2/P3 问题。
- [x] 版本入口一致，发布构建和 Release 工作流所需文件齐全。
- [x] 用户已确认后，按本文件第 9 节执行 commit、merge、push、tag、Release 和必要的分支切换。

## 6. 最小验证与证据要求

### 6.1 代码级验证分层

按照实际改动范围执行最小必要验证：

| 改动范围 | 最小验证 |
| --- | --- |
| 纯几何/状态模型 | `npm.cmd run test:floating-ball`，必要时补充对应 Node 测试 |
| React 悬浮球组件和样式 | `npm.cmd run test:floating-ball`、`npm.cmd run build` |
| Rust 显示器/窗口逻辑 | `cargo fmt --check`、相关 `cargo test`、`cargo check` |
| Tauri capability 或窗口配置 | 上述检查，加对应 capability/桌面启动检查 |
| 发布配置或安装包 | `npm.cmd run tauri:build`、`npm.cmd run verify:loader`，再由用户验收安装/启动 |

不因本轮只涉及悬浮球而重新运行所有预览、库操作和 Sites 测试；只有共享接口或构建契约实际受影响时才追加对应命令。所有无法运行的检查必须在 `PROJECT_PROGRESS.md` 和最终说明中写明原因与影响，不能用“未验证”替代“通过”。

### 6.2 手动验收证据格式

每个阶段的人工结果由用户确认，进度记录采用以下字段：

```text
日期：YYYY-MM-DD
候选版本：0.3.x
系统：Windows 11 版本
WebView2：版本
显示器与 DPI：数量、排列、缩放、负坐标情况
测试夹具：prototype/tests/fixtures/desktop-acceptance/ 下的无敏感夹具
通过项：编号和简短结果
失败项：编号、复现步骤、影响级别
结论：通过 / 返工 / 阻断
```

禁止记录用户真实路径、真实文件内容、访问令牌、私钥、完整外部命令行和包含个人信息的截图索引。

## 7. 版本、进度和文档同步规则

### 7.1 阶段版本表

| 阶段 | 版本 | 主要交付物 | 进入下一阶段的门禁 |
| --- | --- | --- | --- |
| A | `0.3.1` | 悬停/几何/坐标纯模型和测试 | 用户确认几何基线可接受 |
| B | `0.3.2` | 单一悬停状态机、延迟和异步取消 | 用户确认覆盖、过渡和收起稳定 |
| C | `0.3.3` | 四边四角展开、工作区边界和方向回退 | 用户确认边缘没有截断、跳屏和反向展开 |
| D | `0.3.4` | 面板视觉、命中区域、滚动和可访问性 | 用户确认内容和操作无重叠、无误触 |
| E | `0.3.5` | 拖动、多显示器、DPI、恢复和回归 | 用户确认组合行为和生命周期稳定 |
| F | `0.3.6` | 发布候选、最终验收、Git 合并和 Release | 用户明确通过后才执行发布流程 |

### 7.2 版本入口

每个阶段需要实际更新时，必须同时检查：

- `prototype/package.json` 的 `version`。
- `prototype/package-lock.json` 根包 `packages[""].version` 和根包 `version`。
- `prototype/src-tauri/tauri.conf.json` 的 `version`。
- `prototype/src-tauri/Cargo.toml` package 版本和 `Cargo.lock` 根 package 版本。
- `README.md`、`prototype/README.md`、`PROJECT_PROGRESS.md` 中的当前版本、发布说明和已知限制。
- `PROJECT_PLAN.md` 中的阶段状态和最终目标版本。

不能只修改标题或 package 版本后声称完成；Release workflow 会校验 tag 与上述应用版本入口一致。

### 7.3 计划制定阶段的明确边界

本轮用户已明确要求开始实现，因此按本计划修改悬浮球相关源码、测试、版本入口、README 和 `PROJECT_PROGRESS.md`；保留 `.openai/hosting.json`、Sites worker、索引/设置/预览协议和原生权限边界不变。悬浮窗方向按左右位置选择，不再维护垂直弹窗路径。未执行 commit、merge、push、tag、Release 或分支切换，也未使用真实用户资料。

## 8. 风险、优先级和回退

### 8.1 风险优先级

- **P0：**面板不可见、跨屏、窗口消失、应用崩溃、索引丢失、主窗口无法恢复或无法退出。
- **P1：**四边/四角截断、鼠标无法从球体进入面板、持续闪烁、拖动与打开冲突、不同 DPI 下位置严重错误、监听或线程泄漏。
- **P2：**边缘对齐偏差、面板尺寸轻微变化、反馈条样式不一致、焦点返回不理想但仍可操作。
- **P3：**阴影、颜色、微动画或文案细节问题，不影响可见性和操作。

P0/P1 未清零不得进入最终 Release；P2/P3 只有在用户明确接受并记录影响后才可保留。

### 8.2 回退原则

- 优先回退当前阶段的悬停/几何变更，保留已有的文件索引、最近记录、位置文件和预览数据。
- 不删除用户 app data，不清空 `index.json`、`settings.json` 或 `floating-ball.json`；损坏位置只能回到安全默认位置。
- 不使用 `git reset --hard`、`git checkout --` 或强制推送来掩盖失败；需要回退时先记录状态、差异和用户确认的目标。
- 如窗口 API 无法实现稳定的动画或原子调整，优先采用无动画但稳定的显示/定位流程，不引入全局钩子或后台服务。
- 已有 `v0.3.0` 是可回退的稳定版本；若某阶段不能接受，保留该 tag 和当前用户数据，不覆盖既有 Release。

## 9. 用户验收通过后的 Git、合并、推送和 Release 流程

本节是最终门禁，不属于计划制定阶段。只有用户明确回复阶段 F 手动验收通过后，才按顺序执行；任何失败都暂停并记录，不强制覆盖远程状态。

### 9.1 验收后提交

在当前开发分支 `dev` 上执行，提交前确认没有无关文件、真实资料、构建产物、日志、密钥或临时文件：

```powershell
git status --short --branch
git diff --stat
git diff --check
git diff -- PROJECT_PLAN.md PROJECT_PROGRESS.md prototype/src prototype/tests prototype/src-tauri
git add PROJECT_PLAN.md PROJECT_PROGRESS.md prototype/src prototype/tests prototype/src-tauri
git commit -m "feat: 优化悬浮球悬停弹窗"
```

如果 `git diff` 发现无关改动，不应直接 `git add .`；先按文件范围处理并向用户说明。用户要求“验收完后 commit”，因此未验收前不创建功能提交。

### 9.2 合并到 `main` 并推送

```powershell
git switch main
git pull --ff-only origin main
git merge --no-ff dev -m "merge: 合并悬浮球悬停弹窗优化"
git diff HEAD^ HEAD --check
git push origin main
```

- 若 `git pull --ff-only` 或合并发生冲突，停止，不使用强制推送或自动丢弃用户改动。
- 合并提交必须包含已验收的 `0.3.6` 版本入口和同步文档；不能在 `main` 上临时修改版本后再发布。
- `dev` 的同步推送在 Release 验证后执行，保持用户要求的“发布完成再切回 dev”顺序。

### 9.3 创建 tag 并触发 Release

在 `main` 合并提交已推送且版本校验通过后：

```powershell
git tag -a v0.3.6 -m "本地资料工作台 v0.3.6"
git push origin v0.3.6
```

`.github/workflows/release.yml` 会由 `v0.3.6` tag 触发 Windows x64 构建和 GitHub Release。需要检查：

- Action 版本校验确认 `package.json`、`package-lock.json`、Tauri 配置和 Rust crate 都是 `0.3.6`。
- Windows x64 Tauri 构建成功，NSIS 安装包和便携 ZIP 均生成。
- `WebView2Loader.dll` 与 release 主程序布局符合既有检查，Release 不包含测试夹具、用户资料、日志、缓存或签名私钥。
- GitHub Release `v0.3.6` 已正式发布，安装包和便携 ZIP 资源均可下载；Release 说明如实保留 WebView2 Runtime、LibreOffice 和安装签名边界。
- 用户已完成 Release 候选安装/启动检查并确认通过；CI runner 不能替代 Windows 11 桌面验收。

### 9.4 Release 完成后切回 `dev`

```powershell
git switch dev
git push origin dev
git status --short --branch
git log --oneline --decorate -4
```

最终状态必须满足：工作分支为 `dev`，`main` 已包含并推送合并提交，`v0.3.6` tag 已推送，GitHub Release 已验证；若工作区仍有未提交内容，必须在最终进度中说明，不得声称流程完成。

## 10. 最终 Definition of Done

本计划只有在以下条件全部满足后才可关闭：

- [x] 阶段 A-F 的实现范围、版本、代码级验证和用户手动验收记录完整。
- [x] 悬浮球在覆盖、靠近、进入面板、离开、拖动、记录和错误状态下没有未处理的状态竞态。
- [x] 四边、四角、任务栏、多显示器、负坐标和 DPI 情况均经过用户 Windows 11 手动验收。
- [x] 最近记录、收藏、索引、预览、设置、托盘、主窗口和浏览器回退没有回归。
- [x] 所有版本入口一致为 `0.3.6`，并保留 `0.3.0` 的历史发布记录。
- [x] `PROJECT_PROGRESS.md` 已记录实际命令、用户验收状态和 Release 结果。
- [x] 用户验收后已按顺序完成 commit、合并 `main`、推送、tag/Release，并最终切回 `dev`。
- [x] 未提交真实资料、密钥、日志、缓存、构建产物或无关改动。

### 当前执行入口

本计划已完成；后续新功能或版本应新建对应变更计划，并保留 `v0.3.6` 作为当前发布基线。
