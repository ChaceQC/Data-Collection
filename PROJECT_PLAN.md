# 本地资料工作台阶段计划与版本评估

> 更新时间：2026-08-29
>
> 当前版本：0.1.0
>
> 当前阶段：阶段 F（P3）已完成，进入版本评估
>
> 计划状态：P0-P2 和 P3 基础设置/显式外部操作均已完成开发侧实现与 Windows 手工验收；标签、批量和通用撤销栈延期

## 1. 计划变更与依据

旧版 `PROJECT_PLAN.md` 主要描述 Tauri 外壳、文件索引和多格式预览的实现过程。上述内容已经完成，Windows 桌面端的预览验收也已由用户完成，因此旧计划已删除，不再重复安排预览实现或桌面预览验收。

本文件是新的唯一执行入口，依据以下项目资料和最新反馈重新制定：

- `AGENT.md`：local-first 边界、索引存储、路径安全、文件操作确认、测试和文档同步要求。
- `PROJECT_PROGRESS.md`：当前索引、目录浏览、预览适配器、构建和既有验收记录。
- `prototype/README.md`：当前技术基线、运行方式、数据边界、预览依赖和已知限制。
- `prototype/AGENTS.md`：React/Tauri 文件边界、模态预览约束和 Sites 构建契约。
- `prototype/design-qa.md`：收纳入口的视觉基线已经通过，后续功能不能破坏首屏布局和交互。
- 用户最新反馈：预览、资料库核心功能和阶段 F 基础设置/显式外部操作均已由用户完成 Windows 桌面验收；未纳入的增强能力另行延期。

### 本计划的执行原则

1. 先补齐资料库可持续使用所需的核心操作，再扩展搜索、排序、分组和设置。
2. 新功能必须接入真实 Tauri 数据流和 `index.json`，不能只修改浏览器演示数据。
3. “删除”默认解释为从本地资料库索引中移除记录，不删除用户原文件。删除原文件是另一项高风险功能，必须单独设计和确认。
4. 已通过验收的预览能力只作为基线；除非新功能造成回归，不重新安排整套预览验收。
5. 代码、测试和文档均保持中文项目约定；不提交真实资料、完整用户路径、日志、密钥或构建产物。

### 本轮行为变更记录

- `copy_indexed_file` 按最新反馈改为 Windows 文件剪贴板操作：只把当前索引文件以 `CF_HDROP` 写入系统剪贴板，不选择目标目录、不创建副本、不新增索引记录；该行为变化已同步到 command、界面、README 和验收项。它不改变索引格式，开发版本继续保持 `0.1.0`。

## 2. 当前基线和明确缺口

| 能力 | 当前状态 | 证据或实现位置 | 下一步判断 |
| --- | --- | --- | --- |
| Tauri 2 外壳、窗口和原生选择器 | 已完成 | `prototype/src-tauri/`、`prototype/src/App.jsx` | 只做回归检查 |
| 文件/文件夹索引、目录浏览、分页、失效路径重新定位 | 已完成 | `filesystem`、`storage`、`index_paths`、`list_directory`、`reposition_file` | 保持现有协议 |
| 多格式预览和模态对话框 | 已完成并由用户验收 | `prototype/src-tauri/src/preview/`、`prototype/src/features/preview/` | 新功能只验证不回归 |
| `favorite` 字段和收藏筛选视图 | 已完成并由用户验收 | `IndexEntry.favorite`、`features/library/`、`set_favorite` | 保持现有协议 |
| 收藏状态持久化 | 已完成并由用户验收 | `storage::update_entries`、`commands::index_paths`、`commands::reposition_file` | 保持现有协议 |
| 从资料库移除记录 | 已完成并由用户验收 | `remove_index_entry`、`RemoveIndexDialog` | 保持现有协议 |
| 搜索 | 已完成并由用户验收 | `features/library/libraryModel.js`、`LibraryPanel.jsx` | 保持现有协议 |
| 最近添加 | 已完成并由用户验收 | `IndexEntry.addedAt`、索引版本 2 迁移、资料库排序 | 保持现有协议 |
| 排序、分组 | 排序已完成，分组未实现 | `libraryModel.js`、`LibraryPanel.jsx` | 分组列入后续候选 |
| 设置 | 基础设置已实现并由用户验收，预览限制只读 | `storage/settings.rs`、`features/settings/`、`settings.json` | 保持字段白名单和原子写入 |
| 阶段 F 外部打开 | 已实现并由用户 Windows 手工验收 | `open_indexed_file`、`reveal_indexed_file`、`filesystem/external.rs` | 只通过 Rust 白名单 command 实现，并由用户明确触发 |
| 浏览器/Sites 回退 | 仅使用内存演示数据，不接触真实文件 | `prototype/README.md`、Sites 测试 | 新增交互时保持演示可用 |

当前索引格式为版本 `2`，包含 `favorite` 和 `addedAt` 字段。版本 `1` 会按 `modifiedAt` 回填 `addedAt` 后原子迁移到版本 `2`；损坏或未知版本会报告错误，不能因版本不一致直接清空用户索引。

## 3. A-E 已完成目标和边界

### 3.1 目标

- 收藏和取消收藏资料库中的文件、文件夹和失效记录，并在重启、刷新索引、重复导入和重新定位后保持状态。
- 允许用户把一条记录从本地资料库移除，确认后只修改 `index.json`，原文件、文件夹和其中的内容保持不变。
- 建立真正可用的资料库视图：搜索、筛选、排序、分页、选择状态和空状态能够协同工作。
- 修正“最近添加”的语义，使其来自持久化元数据而不是简单排除失效路径。
- 在核心索引能力稳定后，增加受控的原文件复制、重命名和删除操作，并明确每个操作的影响范围。
- 将新增逻辑拆到资料库 feature 和 Rust storage/command 模块中，避免继续扩大 `App.jsx` 的职责。
- 为核心操作补齐成功、取消、未知 ID、索引写入失败、路径失效、快速切换和应用重启场景的验证。
- 保持已验收的预览、安全边界、目录浏览、浏览器回退和 Sites 构建契约不变。

### 3.2 非目标

- P0/P1 核心阶段不直接修改用户原文件；原文件复制、重命名和删除列入后续阶段 E，必须通过独立确认、路径复核和 Windows 手工验收后才能开启。
- 本计划不实现文件夹删除、移动或批量原文件操作；这些操作会影响更多路径，另行设计。
- 不实现全文检索；当前索引只保存路径和元数据，搜索先覆盖文件名、类型和状态。
- 不引入云同步、账号、服务器 API、遥测、SQLite 或未登记的后台服务。
- 不为了加入收藏或删除而放宽 Tauri capability、资源协议、路径校验或预览安全限制。
- 不重做已经通过设计验收的收纳入口首屏，不把功能控件随意堆入拖放区域。
- 不重新实现或扩大未经过证据验证的文件格式支持范围。

### 3.3 阶段 F 目标（已完成）

- 实现可原子持久化的基础设置，并让默认排序、分页和索引移除确认偏好在重启后恢复。
- 让用户通过明确的资料行操作调用受控的系统默认程序或 Windows 资源管理器，不接受任意路径、命令或 URL。
- 完成标签/分组、批量操作和撤销/历史的产品边界评估；未定义稳定数据模型、影响预览和恢复语义前不实现入口。

## 4. 产品行为约定

### 4.1 收藏

- 收藏是资料库元数据，不改变原文件，不创建副本，也不读取文件正文。
- 收藏操作针对持久化索引项，首版覆盖资料库根列表中的文件、文件夹和失效记录。
- 文件夹内部由 `list_directory` 返回的子项目前是临时目录视图，不单独写入索引；首版不在目录子项上显示可持久化收藏操作。
- 行操作使用现有 `@phosphor-icons/react` 图标，按钮必须有明确的 `aria-label`、`title` 和键盘操作，不与整行打开预览的点击事件冲突。
- 收藏视图实时反映变化，取消最后一条收藏后显示明确的空状态；当前选中项、分页和预览对话框不应被无故重置。

### 4.2 删除

- 界面文案使用“从资料库移除”或等价的明确表述，避免让用户误以为会删除磁盘文件。
- 用户触发前展示影响范围：资料名称、类型和“只移除本地索引，原文件不受影响”的说明；取消不会改变任何数据。
- 首版允许移除有效记录和失效记录。移除文件夹记录只移除该条文件夹索引，不递归处理目录内容。
- 预览对话框打开时不能直接移除底层记录；先关闭并释放 `previewId`，再执行索引操作，避免资源会话悬挂。
- 删除命令只接收 `fileId`，不接收前端传来的文件路径，不启动 shell，不调用文件系统删除 API。
- 删除成功后从资料库、最近添加、收藏和失效视图同步移除，修正选中项和当前页；应用重启后记录仍不应恢复。
- 操作失败时保留原记录，显示可执行的错误提示，不把内部路径、堆栈或存储文件内容展示给用户。

### 4.3 搜索、排序和最近添加

- 搜索入口放在资料库列表区域，不改变已验收的收纳入口主布局；搜索默认作用于当前资料库视图，也可作用于收藏和失效视图。
- 首版搜索字段为名称、显示类型和状态，不搜索正文，不通过预览 command 读取额外内容。
- 搜索输入使用规范化大小写和空白处理，支持中文、空格、非 ASCII 文件名；输入变化后页码回到第一页，但保留合理的选中项。
- 排序至少支持名称、修改时间、文件大小和添加时间，并提供正序/倒序；排序必须稳定，不能因刷新跳到无关记录。
- “最近添加”使用持久化 `addedAt`，重复索引已有路径只刷新元数据，不把它伪装成新添加；旧记录迁移时必须有明确的回填规则。
- 文件夹浏览仍由目录 command 提供直接子项，不把搜索或删除操作误作用到未持久化的临时子项。

### 4.4 原文件操作

原文件操作和“从资料库移除”必须使用不同的 command、不同的按钮文案和不同的确认流程。操作 command 只接收资料库记录 ID，重命名另接收新文件名；Rust 端必须从当前索引重新取得并校验源路径。

- **删除原文件**：首版只支持普通文件，不支持文件夹。Windows 上优先移动到回收站，不做永久删除，不调用 `cmd.exe`、PowerShell 或任意 shell 命令。执行前显示文件名、类型、大小、所在位置和“原文件将被移入回收站”的影响说明；用户取消不产生任何变化。
- **复制文件**：首版只支持普通文件。用户点击操作后，Rust 端只接收 `file_id`，从当前索引重新取得并校验源文件，再通过 Windows `CF_HDROP` 文件剪贴板写入该路径。应用不选择目标目录、不创建副本、不修改索引；用户在资源管理器中自行选择目标目录并粘贴。剪贴板不可用时显示明确失败原因。
- **重命名文件**：首版只支持同一父目录内的普通文件。前端只传入新文件名，不传新路径；Rust 端拒绝路径分隔符、`.`、`..`、Windows 保留名、非法字符、空名称、同名冲突和扩展名变化，避免通过改名伪造文件类型。记录 ID、收藏状态和其他用户元数据保持不变。
- 删除和重命名都必须在预览资源释放或旧任务结果已丢弃后执行，复制在后台设置系统剪贴板；操作完成后保持索引和当前选择状态稳定。
- 文件系统操作必须在后台任务执行，支持失败状态和尽力清理临时文件；不得阻塞窗口线程，不得把完整源路径、正文或系统命令写入普通日志。
- 如果物理删除成功但索引原子保存失败，不能假称操作完全成功：保留可诊断的索引状态，下一次刷新将该记录标记为失效，并在界面提示“原文件已移入回收站，索引仍需处理”，不恢复或伪造原文件。
- 文件夹记录和目录临时子项首版不显示原文件删除、复制和重命名按钮，避免把目录操作误扩展为递归文件系统操作。

## 5. 分阶段实施清单

### 阶段 A：索引状态和命令基础

目标：在实现 UI 之前，建立可测试的持久化变更边界。

- [x] 在 `prototype/src-tauri/src/storage/mod.rs` 提取按 ID 更新和移除索引项的内部辅助逻辑，统一执行快照、变更、原子保存和内存状态替换。
- [x] 在 `prototype/src-tauri/src/commands/mod.rs` 增加 `set_favorite(file_id, favorite)` command；参数只包含 ID 和目标布尔值，返回更新后的索引或明确的变更结果。
- [x] 增加 `remove_index_entry(file_id)` command；只从索引向量和 `index.json` 移除记录，不能调用 `remove_file`、`remove_dir` 或 shell。
- [x] 为未知 ID、空 ID、重复请求、收藏值不变、索引写入失败和保存后内存替换失败定义稳定错误行为。
- [x] 在 `prototype/src-tauri/src/lib.rs` 注册 command，并同步最小必要的 capability/自动生成权限文件；不额外开放文件系统删除权限。
- [x] 审查 `index_paths`、`reposition_file` 和 `load_file_index`，确保刷新、重复导入和重新定位不会覆盖用户收藏状态。
- [x] 仅在排序和最近添加确实需要时扩展 `IndexEntry`。已将 `INDEX_FORMAT_VERSION` 从 `1` 迁移到 `2`，支持读取旧版本、原子写回和失败回滚，禁止版本不匹配后静默清空索引。
- [x] 在 Rust 单测中覆盖：收藏写入后重新读取、取消收藏、移除后重新读取、未知 ID、中文和空格路径记录、失效记录移除、文件原路径仍存在，以及迁移失败不破坏旧索引。

阶段 A 完成条件：命令契约稳定，磁盘索引和内存快照一致，测试能证明“移除记录不等于删除原文件”。

### 阶段 B：收藏功能

目标：让已有的收藏导航从静态筛选变成真实可用功能。

- [x] 在 `prototype/src/features/library/` 新增资料行操作或收藏按钮组件，避免继续把所有行操作分支写入 `App.jsx`。
- [x] 在 `prototype/src/features/library/libraryApi.js` 或现有 API 层封装 `set_favorite`，统一处理 Tauri 和浏览器回退模式。
- [x] 浏览器回退使用内存状态模拟收藏切换，但不得声称已经持久化本地文件；Sites 构建仍不能调用 Tauri command。
- [x] 行按钮在收藏和取消收藏之间切换图标、标签和 `aria-pressed`，点击按钮时阻止事件冒泡，避免同时打开预览。
- [x] 切换期间防止同一记录重复提交；成功后只更新对应记录，失败后恢复原状态并显示提示。
- [x] 收藏视图、资料库视图、最近添加视图和失效路径视图的筛选结果、数量、空状态和分页同步更新。
- [x] 预览打开时切换收藏不应关闭预览；切换文件或关闭预览仍按现有协议释放资源。

阶段 B 手动验收：

- [x] 在资料库中收藏和取消收藏普通文件、文件夹和失效记录。
- [x] 切换到“收藏”视图确认记录即时出现或消失，最后一条取消后显示空状态。
- [x] 重启应用、重新索引同一路径、重新定位失效记录，确认收藏状态按产品约定保留。
- [x] 快速连续点击、键盘操作和预览打开状态下操作，确认没有重复请求、错误选中或旧状态覆盖。

### 阶段 C：从资料库移除记录

目标：提供安全、可理解、可持久化的“删除记录”能力。

- [x] 在 `prototype/src/features/library/` 增加移除操作和确认对话框，显示单条影响范围以及原文件保留说明。
- [x] 只对持久化索引项显示移除操作；目录临时子项首版不显示该操作，避免用户误以为可以直接修改文件夹内容。
- [x] 移除前处理当前预览：调用现有 `dispose_preview`，等待或丢弃过期结果，再提交 `remove_index_entry`。
- [x] 移除成功后更新列表、导航筛选、搜索结果、当前页和选择焦点；移除当前唯一记录后回到清晰的空状态。
- [x] 取消确认、命令失败、未知 ID 和应用关闭中断均保留原记录，不清空整个列表。
- [x] 对失效路径提供同样的移除入口，避免用户只能重新定位而不能清理旧记录。
- [x] 对文件夹记录明确“只移除文件夹在资料库中的记录”，不遍历、不递归、不删除目录下内容。
- [x] 在 Windows 手工验收中确认磁盘上的测试文件仍存在，索引重启后不再出现被移除记录。

阶段 C 手动验收：

- [x] 点击取消不会改变列表和磁盘文件。
- [x] 移除普通文件、文件夹和失效记录后，各导航视图都同步消失。
- [x] 移除正在预览的文件后，对话框关闭、资源释放、主窗口仍可操作。
- [x] 关闭并重启应用，确认移除结果已经持久化；重新导入同一个原文件时可以作为新记录登记。
- [x] 使用中文文件名、空格路径和深层目录确认没有误删原文件或误删相邻记录。

### 阶段 D：完整资料库视图

目标：把当前收纳入口扩展为可长期管理的资料库，不破坏已验收首屏。

- [x] 将 `searchQuery` 的输入控件接入资料库区域，提供清空操作、匹配数量、无结果和加载状态。
- [x] 将筛选、排序和分页抽成可测试的纯函数或 feature hook，减少 `App.jsx` 同时承担数据、交互和渲染的职责。
- [x] 增加排序菜单或分段控件，至少覆盖名称、修改时间、添加时间和大小；记录当前排序，刷新后保持稳定。
- [x] 为真实“最近添加”扩展索引模型加入 `addedAt`，并在新导入、重复导入、失效刷新和重新定位时遵守一致规则。
- [x] 为导航项显示实际数量时，数量来自当前索引状态，不使用演示数据硬编码。
- [x] 检查收藏、失效路径、搜索和排序组合条件；清空搜索后恢复原视图，切换导航后不残留旧预览或错误分页。
- [x] 保留每页 20 条、目录面包屑、刷新后选择状态和文件夹直接子项行为；相关改动已增加回归检查。
- [x] 保持首屏主色和已确认文案；按用户后续反馈将拖放区改为基于视口的自适应高度，并让资料列表成为主滚动区域。

阶段 D 手动验收：

- [x] 用中文名、空格名、大小写混合名和无结果关键词测试搜索。
- [x] 在资料库、收藏、失效路径和最近添加视图中分别验证搜索、排序、分页和空状态。
- [x] 超过 20 条记录时切换排序、收藏和移除，确认页码、焦点和结果数量正确。
- [x] 进入和返回文件夹后确认搜索和资料库状态不会误修改目录临时数据。

### 阶段 E：受控原文件操作

目标：在收藏和索引管理稳定后，提供用户明确触发的复制、重命名和删除原文件能力。该阶段只处理单个普通文件，不处理文件夹、批量任务或移动操作。

#### E1. 复制到系统剪贴板

- [x] 在 `prototype/src/features/library/` 增加“复制到剪贴板”行操作；不打开目标目录选择器，复制成功后原记录、当前索引和选择状态保持不变。
- [x] 增加 `copy_indexed_file(file_id)` command；只接收记录 ID，从 Rust 当前索引取得源路径，不接收前端传入的目标路径。
- [x] 校验源文件是存在且可读的普通文件，拒绝源路径中的 symlink、Windows reparse point、特殊文件和目录外跳转。
- [x] 使用项目内锁定的 `windows-sys` 调用 Windows `CF_HDROP` 文件剪贴板，支持中文文件名、空格路径和资源管理器粘贴；不复制正文、不创建临时副本、不改写索引。
- [x] 剪贴板忙、内存分配失败、系统 API 失败和非 Windows 平台均返回明确错误；文件路径只在用户明确触发时写入系统剪贴板。
- [x] 返回结构化结果中的文件名和成功状态，不返回内部命令、堆栈或无关系统信息；剪贴板操作不会改变 `addedAt`、收藏或记录 ID。

#### E2. 重命名文件

- [x] 增加 `rename_indexed_file(file_id, new_name)` command；前端只传新文件名，不传新路径或拼接后的完整路径。
- [x] Rust 端使用当前索引路径的父目录构造目标路径，并拒绝空名称、`.`、`..`、路径分隔符、Windows 非法字符、保留设备名、同名目标和超长名称。
- [x] 首版要求扩展名保持不变，避免重命名同时改变预览类型；扩展名变更作为另一个产品决策，不在本阶段隐式允许。
- [x] 只允许同一父目录内的普通文件重命名；文件夹和目录临时子项不显示该操作。
- [x] 使用不覆盖目标的文件系统改名操作，在后台执行；成功后重新读取元数据，保留原记录 ID、收藏状态和索引中的其他用户字段。
- [x] 重命名失败时保留原记录和原文件，区分目标已存在、无权限、文件不存在和系统占用等可执行错误。
- [x] 重命名中的记录 ID 不能根据新路径重新生成，否则会破坏收藏、选中状态和其他记录关联；新路径只更新 `path`、`name` 和必要的文件元数据。

#### E3. 删除原文件

- [x] 增加独立的 `delete_original_file(file_id)` command，不能复用 `remove_index_entry`，不能把索引移除按钮升级成物理删除。
- [x] 首版只支持普通文件，Windows 上使用项目内锁定的 `trash` 回收站库；禁止永久删除，禁止调用 `cmd.exe`、PowerShell 或任意 shell。
- [x] 删除前显示二次确认：文件名、类型、大小、所在位置、回收站行为和“该操作会影响磁盘上的原文件”。取消、关闭对话框或路径重新校验失败都不产生变化。
- [x] Rust 端从 `file_id` 找到源路径后重新 canonicalize、检查普通文件、拒绝 symlink/reparse point；前端不能传入替代路径绕过校验。
- [x] 删除正在预览的文件时，先关闭预览并释放 `previewId`，再执行物理操作；旧的异步预览结果不得重新写入已删除记录。
- [x] 回收站操作成功后再原子更新索引；若索引保存失败，返回“原文件已移入回收站但索引未同步”的明确状态，下一次刷新将缺失路径标记为失效，不能假称两步都成功。
- [x] 删除成功后从资料库、最近添加、收藏和失效视图移除记录；原文件只能通过用户自己的回收站操作恢复，应用不自动复制或恢复内容。
- [x] 文件夹删除、永久删除、批量删除和跨卷移动不在本阶段实现。

#### E4. 共同行为和测试

- [x] 三种操作分别使用清晰文案和图标：`复制到剪贴板`、`重命名文件`、`删除原文件`；`从资料库移除`继续只表示索引移除。
- [x] 操作期间禁用同一条记录的重复提交，保持其他行可用；成功后只刷新受影响的记录和必要的列表状态。
- [x] 更新 capability 时仅开放新增的业务 command，不开放任意路径删除、任意 shell 或任意外部进程权限。
- [x] 为复制、重命名和删除增加 Rust 测试：中文/空格路径、`CF_HDROP` UTF-16 双零结尾、非法新名称、失效路径、预览资源释放和索引写入失败；权限不足以稳定构造时使用不可访问/不存在路径的错误映射覆盖。
- [x] 使用临时无敏感文件验证原文件操作；测试完成后清理临时目录，不把测试文件、回收站路径或完整用户路径提交到仓库。

阶段 E 手动验收：

- [x] 在资料库点击“复制到剪贴板”，在 Windows 资源管理器目标目录粘贴，确认源文件内容不变、剪贴板中的中文/空格路径正确，且索引不会新增副本记录。
- [x] 在剪贴板被其他程序占用或复制文件失效时执行操作，确认显示明确错误，源文件和索引均不发生额外变化。
- [x] 重命名文件后确认文件名、索引记录和预览类型正确，收藏状态与选中状态仍保留；重启后路径仍有效。
- [x] 使用非法字符、路径分隔符、同名目标和扩展名变化测试重命名失败提示。
- [x] 删除原文件后确认文件进入回收站、索引记录消失、预览资源释放，且应用不会删除相邻文件或目录。
- [x] 取消删除、删除失效记录、关闭预览后删除和快速重复操作，确认不会发生物理文件误删或索引损坏。

### 阶段 F：设置、外部打开和后续增强评估

本阶段只在 A-E 稳定后开始，避免把尚未稳定的基础操作和低优先级功能混在一起。

- [x] 将当前“设置入口将在 Tauri 接入后开放”的占位行为替换为真实设置面板，并同步 README，不能保留虚假入口。
- [x] 评估并实现本地设置：默认排序、每页数量和索引移除确认偏好持久化到独立的 `settings.json`；预览限制只读展示，不能越过统一 Rust 安全上限。
- [x] 增加显式用户触发的 `open_indexed_file` 和 `reveal_indexed_file` 白名单 command；默认不静默打开外部程序或 URL，路径由 Rust 从索引重新取得并校验。
- [x] 完成标签/用户分组、批量收藏和批量移除的数据模型、影响范围和撤销策略评估；本阶段不实现这些入口，决策记录在 `docs/phase-f-settings-and-external-operations.md`。
- [x] 完成原文件操作的撤销提示、操作历史和失败恢复评估；物理删除继续只使用系统回收站语义，不承诺应用无条件恢复。

#### 阶段 F 实施决策

- 设置格式版本为 `1`，与索引版本 `2` 分离；设置缺失时创建安全默认值，损坏或非法设置回退默认值且不清空索引。
- `confirmBeforeRemove` 只控制从资料库移除索引记录的二次确认；删除原文件的影响范围确认始终强制开启。
- 预览大小和图片像素限制由统一 Rust 配置提供，设置 command 不接受前端传入的限制字段。
- 外部打开只覆盖已登记的普通文件；资源管理器定位覆盖已登记的普通文件和文件夹。浏览器/Sites 模式只在当前会话模拟设置，不访问真实文件或外部程序。
- 标签/分组、批量操作和通用撤销栈延期，不因为阶段 F 的设置或外部打开而修改索引格式。

## 6. 预期文件变更

### 第一批必须修改

- `prototype/src-tauri/src/storage/mod.rs`：索引按 ID 更新、移除、原子保存和数据迁移。
- `prototype/src-tauri/src/storage/settings.rs`：独立设置格式、原子保存、默认值和字段校验。
- `prototype/src-tauri/src/commands/mod.rs`、`prototype/src-tauri/src/commands/library.rs`：索引/预览入口、资料库操作 command 及统一错误映射。
- `prototype/src-tauri/src/commands/settings.rs`、`prototype/src-tauri/src/filesystem/external.rs`：设置 command 和受控 Windows 外部操作适配器。
- `prototype/src-tauri/src/lib.rs`：注册新增 command。
- `prototype/src-tauri/build.rs`：把新增 command 纳入 Tauri 构建命令清单。
- `prototype/src-tauri/capabilities/default.json`：只增加实际需要的业务 command 权限，不开放任意路径删除或任意 shell 能力。
- `prototype/src-tauri/permissions/autogenerated/`：新增业务 command 的 allow/deny 权限文件。
- `prototype/src/App.jsx`：保留页面编排，接入资料库 feature 的状态和回调。
- `prototype/src/features/library/`：新增 `LibraryPanel`、`LibraryActions`、`libraryModel` 和 `libraryApi`，承载行操作、确认对话框、搜索/排序状态和 API 封装。
- `prototype/src/features/settings/`：设置面板、字段规范化和 Tauri/浏览器回退 API。
- `prototype/src/styles.css`：增加行操作、确认对话框、搜索/排序、操作进度和空状态样式，保持设计基线。
- `prototype/index.html`、`prototype/public/favicon.svg`、`prototype/.gitignore`：补充页面图标并忽略浏览器验证临时产物。

### 视数据模型需要修改

- `prototype/src-tauri/src/filesystem/mod.rs`：仅在增加 `addedAt` 或其他索引元数据时修改 `IndexEntry` 和构造逻辑。
- `prototype/src-tauri/src/filesystem/operations.rs`、`prototype/src-tauri/src/filesystem/clipboard.rs`：路径复核、文件名校验、Windows 文件剪贴板和原文件操作的后台辅助逻辑。
- `prototype/src-tauri/src/config.rs`：预览大小和图片像素统一限制，供预览执行与设置只读展示共用。
- `prototype/src-tauri/src/storage/mod.rs`：索引格式版本迁移和旧数据回填。
- `prototype/src-tauri/Cargo.toml`、`prototype/src-tauri/Cargo.lock`：锁定并记录 `trash` 回收站库和 MIT/Apache `windows-sys` Windows API 依赖。
- `prototype/package.json`、`prototype/package-lock.json`：只有实际引入前端依赖时修改，不为简单确认框重复引入 UI 框架。

### 测试和文档

- `prototype/src-tauri/src/storage/mod.rs`、`prototype/src-tauri/src/commands/mod.rs`、`prototype/src-tauri/src/filesystem/operations.rs` 或对应 Rust 测试模块：命令、持久化、路径校验、回收站、复制、重命名和迁移测试。
- `prototype/tests/library-model.test.mjs`、`prototype/tests/`：新增无敏感的库操作测试、纯函数测试和临时文件操作测试；不提交真实用户文件。
- `prototype/README.md`：更新收藏、索引移除、搜索、原文件操作、当前限制和版本状态。
- `docs/phase-f-settings-and-external-operations.md`：记录阶段 F 设置模型、command 契约、权限边界和延期决策。
- `PROJECT_PROGRESS.md`：记录每一阶段的实际完成项、失败项、命令结果和用户手工验收结果。
- `PROJECT_PLAN.md`：只维护尚未完成的路线，不把设计草案勾选成已完成。

以下 Sites 契约文件保持不变，除非测试明确证明需要调整：

- `prototype/.openai/hosting.json`
- `prototype/worker/index.js`
- `prototype/scripts/prepare-sites-build.mjs`
- `prototype/tests/sites-worker.test.mjs`

## 7. 最小验证矩阵

### 阶段 A-E 实现期间

按改动范围执行最小必要验证：

- Rust storage/command/索引格式改动：在 `prototype/src-tauri` 执行 `cargo fmt --check`、`cargo test`，必要时执行 `cargo clippy --all-targets --all-features -- -D warnings`。
- 前端收藏、移除、搜索、排序改动：在 `prototype` 执行对应前端测试和 `npm.cmd run build`。
- 复制、重命名、删除原文件或文件系统辅助逻辑改动：增加临时无敏感文件测试、目标冲突/非法名称测试、回收站结果测试和资源释放测试；不得用真实用户资料验证。
- Sites 行为或构建入口改动：追加 `npm.cmd run test:sites`，确认 `dist/client/index.html`、`dist/server/index.js` 和 `dist/.openai/hosting.json` 仍生成。
- Tauri capability、Windows 原生操作依赖、窗口配置或安装配置改动：追加 `npm.cmd run tauri:build`，否则不为普通 UI 变更重复构建安装包。
- 真实 Windows 行为：由用户使用无敏感夹具手工验收；开发侧不以浏览器演示或 Rust 单测代替桌面结论。

### 阶段 F 实现期间

- 设置模型、字段校验和统一预览限制：在 `prototype` 执行 `npm.cmd run test:settings`，在 `prototype/src-tauri` 执行 `cargo fmt --check`、`cargo test` 和必要的 `cargo clippy`。
- Tauri command、路径访问和 capability 改动：追加 `cargo check`；Windows 外部打开和资源管理器定位由用户使用无敏感夹具手工验收。
- 设置/外部操作前端改动：执行 `npm.cmd run build`、`npm.cmd run test:settings` 和既有库/预览/Sites 回归测试。

### 每一阶段提交前

- 检查 `git status --short` 和改动内容，只保留当前功能相关文件。
- 执行 `git diff --check`；对未跟踪文件额外检查行尾空格和异常控制字符。
- 检查 `.gitignore` 未遗漏 `node_modules/`、`src-tauri/target/`、日志、缓存、索引、临时转换目录和真实测试资料。
- 检查普通日志没有完整路径、文件正文、转换命令、令牌或密钥。
- 更新 `PROJECT_PROGRESS.md` 的日期、状态、风险、下一步、涉及文件和验证结果。
- 每个可验证小步单独提交；未经明确要求不改 remote、不推送、不创建 Release。

## 8. 交付顺序和完成门槛

### P0：收藏和索引移除

完成阶段 A-C。必须能在真实 Tauri 应用中操作，数据重启后保持，移除不能影响原文件；失败、取消和快速操作不能破坏索引。

### P1：资料库搜索、排序和最近添加

完成阶段 D。资料库能够按名称/类型/状态搜索、按至少四种字段排序并稳定分页；“最近添加”有可解释的数据来源。

### P2：受控原文件操作

完成阶段 E。复制、重命名和删除原文件分别有独立 command 和确认流程；复制不覆盖、不破坏源文件，重命名不允许路径穿越或扩展名伪造，删除只进入回收站且不支持文件夹和永久删除。

### P3：设置和显式外部操作

完成阶段 F 中确定纳入的基础设置和显式外部操作，且已通过用户 Windows 手工验收。标签/分组、批量操作和通用撤销栈已完成延期评估，不计入 P3 当前实现范围。所有外部程序、资源管理器或 URL 行为都必须由用户明确触发，并经过最小权限审查。

### 版本判断

- 开发过程中继续使用 `0.1.0`，不因计划改写自动升级版本。
- P0、P1、纳入发布范围的 P2 核心功能以及已纳入 P3 的基础设置/显式外部操作均已通过开发侧验证、Windows 手工验收和文档同步；是否统一升级到 `0.2.0` 作为下一步单独版本决策。
- 如果只完成部分功能，保持 `0.1.0`，在 README 和进度中分别标记“已实现”“部分支持”“计划实现”。
- 版本升级时同步检查 `prototype/package.json`、`prototype/package-lock.json`、`prototype/src-tauri/tauri.conf.json`、`prototype/src-tauri/Cargo.toml` 和 `prototype/src-tauri/Cargo.lock`。

## 9. Definition of Done

本轮资料库功能只有同时满足以下条件才算完成：

1. 收藏和取消收藏通过真实 command 工作，持久化、刷新、重复导入、重新定位和重启行为有测试或验收记录。
2. “从资料库移除”有清晰确认和错误状态，只修改索引，不删除、移动、复制或覆盖原文件。
3. 搜索、排序、分页、最近添加和各导航视图的组合状态可解释、可恢复，不显示静态演示数据冒充真实状态。
4. 复制文件能够把源路径写入 Windows 文件剪贴板并由资源管理器粘贴；复制不创建或登记副本，重命名保持记录 ID、收藏状态和预览类型，删除原文件只使用回收站语义。
5. 原文件操作具备路径复核、权限失败、冲突、取消、超时/中断、索引保存失败和临时文件清理处理。
6. 当前预览对话框、资源释放、目录浏览、拖放和 Sites 构建没有回归。
7. Rust 和前端验证按变更范围通过；无法执行的 Windows 手工项明确记录原因和影响。
8. `prototype/README.md`、`PROJECT_PROGRESS.md`、索引格式说明、Tauri 权限和依赖锁文件与实际实现一致。
9. 未提交真实资料、用户路径、密钥、日志、缓存、安装包和临时转换产物。

### 9.1 阶段 F 完成门槛

1. 设置文件与索引分离、版本明确、原子写入；设置损坏时不清空索引并能回退安全默认值。
2. 设置字段由 Rust 白名单校验，预览限制只读返回且不能被前端更新，物理删除确认不能被关闭。
3. 外部打开和资源管理器定位只使用记录 ID、Rust 路径复核和最小 capability；浏览器回退不启动外部程序。
4. 设置重启恢复、默认程序打开和资源管理器定位已通过用户的 Windows 无敏感夹具手工验收，并在 `PROJECT_PROGRESS.md` 记录结果。
5. 标签/分组、批量操作和通用撤销栈的延期理由、数据模型方向和恢复边界有独立文档，不混入单条操作协议。

## 10. 后续候选，不纳入 P3 基础范围

以下内容已完成阶段 F 评估，但不纳入当前 P3 基础实现，也不标记为已实现：

- 正文全文检索和内容索引。
- SQLite、标签体系、用户自定义分组和批量操作。
- 缩略图缓存、重复文件检测和后台增量扫描。
- 文件夹删除、移动、批量复制/重命名/删除，以及永久删除。
- 更多视频容器/编码、复杂 Office 排版和高级 PDF 标注。
- 云同步、账号、在线搜索和远程协作。
