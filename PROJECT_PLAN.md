# 本地资料工作台 0.3.x 缺陷修复与质量门禁实施计划

> 编制日期：2026-09-05
> 计划状态：阶段 A-C 开发侧收口及用户原生验收已完成；阶段 C 已发布；本计划至阶段 C 为止
> 当前发布版本：v0.3.43
> 当前阶段：阶段 C 已交付、验收并发布，版本 0.3.43
> 本轮版本范围：0.3.41 至 0.3.43，每阶段一个独立版本
> 开发与合并：每阶段在独立分支开发，完成后本地快进并入 dev
> 本地交付要求：每阶段必须成功构建对应版本的 Windows x64 NSIS 安装包
> 适用平台：Windows 11 x64、Tauri 2、Rust stable MSVC、React 19、Vite 6

## 1. 计划职责与基线

### 1.1 完整替换旧计划

- 本文件完整替换此前的“0.3.x 架构加固、性能和可维护性实施计划”，不在旧阶段后追加任务，不另存一份仍可执行的旧 PLAN。
- 本轮 A-C 只表示 `0.3.41` 至 `0.3.43`；不得按历史路线中的同名阶段执行。
- 已发布版本、用户已确认的 Windows 原生验收和历史开发记录继续保存在 Git 历史、`PROJECT_PROGRESS.md` 和 README 中。
- `v0.3.40` 的发布和用户验收保持为历史事实。本计划只处理本轮 A-C 范围内的复查问题；未纳入 A-C 的模块拆分、完整契约和 CI 事项不属于本计划执行项。
- 2026-09-05 的规划提交为 `03298ae`，已同步到本地 main / dev；随后阶段 A 在独立分支开始实现，候选版本提升为 `0.3.41`。

### 1.2 当前架构

~~~text
主窗口 App / 悬浮球窗口
  -> React Hook、界面状态和 action service
  -> Repository / IPC runtime validator
  -> Tauri command
  -> Rust 索引、正文索引、文件系统、预览与 Windows 服务
  -> 本地 JSON、用户选定文件、受控临时目录和 LibreOffice
~~~

- 前端实际以 JavaScript / JSX 为主，存在 `ipcContracts.d.ts`，当前没有完整类型检查门禁。
- 主窗口和悬浮球共享 Rust managed state，通过 revision 和窗口事件同步。
- 主索引为 `index.json` v5；正文缓存、设置、操作历史、悬浮球位置和待同步操作分别存储。
- 保留 PDF.js、Mammoth、SheetJS、DOMPurify、共享 `file-types.json`、原子写入、路径复核、单实例和任务释放机制。
- 当前只有标签发布 workflow，现有 parity 只证明命令名称集合一致，不能证明字段契约或分支 CI 完整。

### 1.3 本轮边界

- 修复本次报告中的确定性问题，并补齐相关状态、职责、性能和工程门禁缺口。
- 保持现有 UI 布局、导航语义、快捷键、数据目录、文件操作确认原则和 Windows x64 发布目标。
- 不增加云同步、账号、在线搜索、遥测、自动上传、后台服务或新的批量物理文件操作。
- 不直接迁移 SQLite，不一次性重写全部前端为 TypeScript，不更换成熟预览引擎。
- 新依赖必须安装在项目内、同步锁文件并说明用途，不依赖个人目录中的库或未记录的全局工具。
- 文件读写和终端使用 UTF-8。读取配置、执行审计和记录证据时，不显形输出密钥、访问令牌、私钥、用户正文或真实资料路径。

## 2. 缺陷归属与合并原则

### 2.1 缺陷对照

编号对应 2026-09-05 的代码复查报告。每项都需要代码和行为证据，不能只通过更改完成状态销项。

| 编号 | 问题 | 合并阶段 | 主要位置 |
| --- | --- | --- | --- |
| R01 | 无 Range 的大 PDF 被截为 1 MiB，PDF.js 误判损坏 | A | `preview/resource_protocol.rs`、`PdfPreviewer.jsx` |
| R02 | GB18030 解码后大小通过写入路径，重启读取缓存却被判损坏 | C | `storage/content_index.rs`、`preview/text.rs` |
| R03 | 删除日志停在 prepared 后无法恢复，重建空索引也未处理 pending | B | `commands/library.rs`、`storage/mod.rs` |
| R04 | 重命名提交的旧条目覆盖期间发生的收藏等修改 | B | `commands/library.rs` |
| R05 | 普通文件重定位打开文件夹选择器；排序后按旧位置返回错误条目 | B | `useLibraryActions.js`、`commands/mod.rs` |
| R06 | App 漏传 directoryView，索引同步误清除目录子项预览 | A | `App.jsx`、`useIndexController.js` |
| R07 | 离开目录未废弃在途请求，迟到结果覆盖后续导航 | A | `useLibraryNavigation.js` |
| R08 | 索引同步失败后无退避、无上限地重新请求 | A | `useIndexController.js` |
| R09 | 预览回写绑定全库 revision，无关修改导致状态和最近打开漏记 | A | `PreviewPane.jsx`、预览 command、索引 mutation |
| R11 | 摘要输出三个省略号字符，命中坐标却只加一个字符 | C | `storage/content_search.rs` |
| ARC02 | 高频路径仍整库复制、扫描文件系统和同步搜索正文 | C（正文搜索部分） | 正文搜索、索引 Repository 和状态提交 |
| ARC03 | 字段契约、Hook 参数类型、组件交互和 command 业务覆盖不足 | A-C 定向补充 | `ipcContracts.*`、控制器、测试与构建脚本 |

### 2.2 合并依据

- A 合并预览资源、目录上下文、请求生命周期和预览回写，共同验证“打开资料、切换资料、同步后保持当前视图”。
- B 合并重定位、重命名和删除恢复，共同处理稳定 ID、文件身份、当前用户元数据和提交结果。
- C 合并正文缓存、摘要和查询执行，共用编码边界、正文模型、缓存生命周期及检索回归。

## 3. 阶段总览与依赖

| 阶段 | 版本 | 主题 | 主要归属 | 前置条件 | 开发状态 | 本地安装包 |
| --- | --- | --- | --- | --- | --- | --- |
| A | `0.3.41` | 预览与界面异步状态闭环 | R01、R06-R09 | v0.3.40 基线 | 开发侧收口及用户原生验收已完成 | 已构建并通过 loader 校验 |
| B | `0.3.42` | 文件操作一致性与删除恢复 | R03-R05 | A 开发侧收口 | 开发侧收口及用户原生验收已完成 | 已构建并通过 loader 校验 |
| C | `0.3.43` | 正文索引、摘要与搜索执行 | R02、R11、正文搜索性能 | B 开发侧收口 | 开发侧收口及用户原生验收已完成 | 已构建并通过 loader 校验 |

默认 A -> B -> C 顺序执行，前置条件以实际代码、验证和本地安装包为准。

每个阶段必须在独立分支完成；前一阶段完成开发侧门禁后先并入 `dev`，下一阶段再从更新后的 `dev` 创建新分支。前一阶段尚未并入 `dev` 时，不得开始下一阶段开发。

“开发侧收口”包括代码、自动检查、文档、版本、本地 NSIS、证据和本地 Git 收口。用户原生验收另记字段，尚未确认时只能写“本地候选，原生验收待完成”。阶段 C 原生验收未完成时，不宣称本轮完整验收结束。

## 4. 每阶段统一交付规则

### 4.1 开始阶段

1. 读取 `AGENT.md`、`prototype/AGENTS.md`、本计划、`PROJECT_PROGRESS.md` 和相关 README。
2. 检查分支、HEAD、工作树与前置阶段，确认当前版本，保留用户已有修改。
3. 列出本阶段的代码、测试、契约、文档、权限和版本文件，不顺手修改无关模块。
4. 使用无个人信息的夹具与临时目录，自动验证不操作用户真实资料。
5. 对已复现问题保留最小回归场景，不为了测试数量增加只检查同名转调的测试。

### 4.2 五个版本文件

| 文件 | 必须核对的应用版本 |
| --- | --- |
| `prototype/package.json` | `version` |
| `prototype/package-lock.json` | 顶层 `version` 和 `packages[""]["version"]` |
| `prototype/src-tauri/tauri.conf.json` | `version` |
| `prototype/src-tauri/Cargo.toml` | 根 crate `version` |
| `prototype/src-tauri/Cargo.lock` | `name = "local-material-workbench"` 的根 package `version` |

- 五个文件中的版本、文档阶段号和安装包文件名全部等于本阶段候选版本。
- 使用结构化解析，不能全文件替换第三方依赖版本；只输出需要核对的非敏感字段。
- A-C 均按上述五个文件分别核对版本。
- 阶段内修复后重新构建同阶段候选，记录新的时间和 hash；已经发布的版本不得用不同内容覆盖。

### 4.3 最小必要验证

- A-C 运行对应前端测试、必要 Hook / 组件交互回归和 Rust 领域测试，不无理由重复全量测试。
- Rust command、存储、路径或资源协议变化时执行 `cargo fmt --check`、`cargo check --locked`、`cargo check --tests --locked`、相关 `cargo test --locked` 和严格 clippy。
- IPC 变化必须同步 Rust、前端调用、runtime validator、`.d.ts` 和测试；不能因统一门禁尚未覆盖而漏改。
- 最终前端生产构建必须成功；`tauri:build` 已对同一最终源码完成前端构建时，不额外重复一次相同的 `npm.cmd run build`。
- 已通过检查只在后续修改、失败或未解决风险需要时重跑。

现有前端测试入口如下，按范围选用：

~~~powershell
npm.cmd run test:library
npm.cmd run test:contracts
npm.cmd run test:content
npm.cmd run test:settings
npm.cmd run test:operations
npm.cmd run test:preview
npm.cmd run test:floating-ball
npm.cmd run test:tray
npm.cmd run test:parity
npm.cmd run test:sites
~~~

前端命令在 `E:\Project\test\prototype` 执行，Rust 命令在 `prototype\src-tauri` 执行。`test:sites` 在构建产物已生成后执行。

### 4.4 每阶段强制本地构建安装包

代码、契约和版本同步后，在 `E:\Project\test\prototype` 执行：

~~~powershell
npm.cmd run tauri:build
~~~

当前脚本包含 Tauri 打包和 `verify-webview2-loader.mjs`；独立复核 loader 时运行 `npm.cmd run verify:loader`。每阶段必须满足：

- 本地前端构建、Rust release 编译、Windows x64 NSIS 打包和 loader 校验全部成功。
- 产物位于 `prototype/src-tauri/target/release/bundle/nsis/`，名称为 `本地资料工作台_<阶段版本>_x64-setup.exe`。
- release 主程序版本等于阶段版本，`WebView2Loader.dll` 为 x64 且与主程序同目录。
- 进度中记录安装包绝对路径、大小、SHA-256、构建时间、对应源码提交和 loader 结果。
- 构建后又修改源码、依赖、配置或打包资源，必须重新构建并更新证据。
- 旧安装包、其他版本安装包、仅前端 build 成功均不满足阶段门禁。
- 本地 NSIS 构建失败时阶段保持进行中，不进入下一阶段，不伪造已完成状态。
- 安装包、临时夹具、缓存和日志不提交。保留阶段最终候选和上一可用候选供比较与回退。

### 4.5 原生验收与文档

- 桌面和真实资料验收由用户执行，代理不替代用户操作，也不将浏览器结果称为 Windows 11 / Tauri / WebView2 验收。
- 每阶段分别记录自动验证、本地安装包和用户原生验收状态。
- 每阶段更新本计划与 `PROJECT_PROGRESS.md`；能力、限制、依赖或开发命令变化时同步相关 README / docs。
- 发现原文件误改、元数据丢失、升级启动失败或安装失败，先修复当前阶段并重新打包。
- 临时浏览器或 HTTP 验证仅使用临时高位端口，完成后关闭服务并确认端口释放。

### 4.6 独立阶段分支与 Git 收口

- 每个代码阶段必须从已核实的本地 `dev` 创建独立分支，例如 `codex/phase-0.3.41-preview-state`，用户指定名称时按用户要求；不得直接在 `main` 或 `dev` 开发阶段代码，也不得让多个未收口阶段共用一条分支。
- 本阶段的代码、测试、契约、文档、版本同步和本地安装包构建均在该独立分支完成。阶段通过开发侧门禁并提交后，立即本地并入 `dev`，再开始下一阶段。
- 本次规划文档按用户要求单独提交，将当前 `PROJECT_PLAN.md` 与配套 `PROJECT_PROGRESS.md` 的同一提交同步到本地 `main` 和 `dev`；此文档同步不代表开始代码阶段。
- 提交只包含当前阶段关联文件，提交说明使用英文类型前缀和中文内容。
- 合并前确认 `dev` 是阶段分支祖先，再以 `git merge --ff-only <phase-branch>` 将阶段分支并入 `dev`。
- 若已分叉，先解决同步并重新验证，不强制快进、不重置用户提交、不把失败命令当作已合并。
- 快进不改变已验证代码时可沿用候选；内容改变后必须重新测试和构建本地 NSIS。
- 合并后复核 HEAD 与工作树，再用 `git branch -d` 删除当前已合并阶段分支，不顺手删除其他用户分支。
- 阶段未成功并入 `dev` 或 Git 收口未完成时，不标记开发侧收口，不进入下一阶段。
- 除本次已获授权的规划文档同步外，代码合入 `main`、push、Tag、GitHub Release 与上传仍按用户后续明确要求执行。

## 5. 阶段 A：预览与界面异步状态闭环（0.3.41）

### 5.1 合并范围

合并 R01、R06、R07、R08、R09，先稳定预览与界面状态，为后续操作和重构提供行为基础。

### 5.2 PDF 分段读取

- 在项目现用 PDF.js 上保留有效大文件失败场景，必要对象或尾部 xref 位于首个 1 MiB 之后。
- 优先使用 PDF.js 官方 `PDFDataRangeTransport`，依据 Rust 返回的真实 `byteLength` 建立长度，显式发送受控 Range，跟踪请求并在销毁时中止。
- 不把截断 GET 当完整文件，也不把实际较短 body 的 `Content-Length` 伪改为整份文件长度。
- 单 Range 的 body、`Content-Range`、`Content-Length` 和状态码一致，正确处理 HEAD、尾部范围、越界、空文件和多范围拒绝。
- 无 Range 采用正常完整响应或明确受控拒绝，不能继续静默返回不完整整文件。涉及视频消费者时验证初始请求、进度跳转和错误状态。
- 保留 PDF 大小、页数、页面像素和超时限制；不为协议修复无条件分配整份大视频缓冲区。
- 保留资源授权、路径和文件快照复核；释放后不能继续读取或重新打开被替换的来源。

### 5.3 目录上下文与导航

- `App` 将真实 `directoryView` 传给索引控制器；目录子项选择和预览保留基于当前上下文。
- 索引更新时刷新仍有效的活动目录，只在目标失效、移除或上下文改变时关闭对应预览。
- `selectNav`、`focusEntry`、返回资料库、面包屑和重定位共用目录请求失效处理。
- 旧请求不得修改新导航的目录内容、选中项、loading 或错误状态。
- 请求身份包含根 ID 和相对路径，不能仅靠文件名判断响应归属。

### 5.4 有限同步重试

- 删除失败路径中的无条件递归自调用。
- 一轮总计最多调用 3 次，建议两次重试间隔 250 ms 和 750 ms，常量集中并可用虚拟时钟验证。
- 错误参数、无效契约或不可重试错误立即终止；过期快照也受尝试次数限制，不能转为无限 while。
- 合并在途事件的最大目标 revision，只有满足当前上下文和单调 revision 的结果可应用。
- 失败保留旧列表，提供手动刷新；新合法事件可以发起新一轮，同一轮只给一次最终失败反馈。
- 卸载、取消和新请求使旧回调失效；调用方能识别同步失败，不能继续无条件宣称界面已更新。

### 5.5 文件级预览回写

- 将有效性从“全库 revision 相等”改为“目标仍是本次预览的同一来源且任务有效”。
- 由 Rust 管理受控任务凭证或文件级内存修订标识，关联文件 ID、已验证来源快照和任务身份，前端不传任意可信路径。
- 凭证有容量和失效策略，取消、释放、替换、退出、过期后不能回写；优先保持索引 v5，不为临时凭证增加磁盘字段。
- 无关资料的收藏、标签、分组或导入，不阻止当前预览记录状态与最近打开。
- 目标被移除、重定位、重命名、替换或同文件新任务取代后，旧回写必须拒绝。
- ready 在实际内容可用时记录；失败、超时、取消不得误写最近打开。
- 区分预期过期结果与真实持久化失败，消除“已标记上报、实际失败且永不重试”。
- 同步 preview API、command、runtime validator、`.d.ts` 与事件消费者；目录临时子项仍不调用主索引元数据 mutation。

### 5.6 文件与验证

主要文件：`App.jsx`、`useIndexController.js`、`useLibraryNavigation.js`、`PreviewPane.jsx`、`PdfPreviewer.jsx`、`previewApi.js`、Rust `preview/`、预览 command、索引 mutation、`ipcContracts.*` 和相关测试。

- 使用真实 PDF.js 读取小 PDF、超过 1 MiB 的有效 PDF、尾部对象文件和损坏文件，验证页数、末页内容与释放。
- 证明单请求读取有界，视频 Range 和 DOC 转换 PDF 不回归。
- Hook / 组件测试覆盖读取中导航、索引事件、目录子项保留和目标删除，不能只测独立 revision 比较函数。
- 虚拟时间验证失败终止、间隔、手动恢复、乱序 revision 和卸载。
- 验证慢预览期间悬浮球收藏其他资料仍可记录结果；目标被替换后旧回写仍被拒绝。
- 运行 preview、contracts、相关 library / floating 测试与 Rust 定向检查。
- 五个版本文件统一为 `0.3.41`，本地构建 `本地资料工作台_0.3.41_x64-setup.exe`，通过 loader 校验并记录证据。
- 用户验收：大 PDF / DOC、快速切换、目录内预览、双窗口同时操作、关闭和重试。

### 5.7 本阶段交付记录（2026-09-05）

- R01、R06-R09 的代码与回归落在实现提交 `93a677962939634861a7c10774a01d59b5131069`；文件级凭证不写入磁盘，主索引继续使用 v5。
- 自动门禁：preview 20 项、async-state 8 项、contracts 20 项、library 27 项、floating-ball 30 项及 56 个 command parity 通过；Rust 111 项、fmt/check/check-tests/严格 clippy 通过。
- Edge 使用合成 PDF 和 WebM 验证实际预览组件的末页绘制、canvas 非空像素、视频显式 Range、跳转与失败状态。DOC 的转换 PDF 内容类型走相同 PDF 消费链；真实 LibreOffice 转换及 Windows 桌面验收由用户完成。
- 五个版本文件、安装包和主程序版本均为 `0.3.41`。本地 NSIS 于 `2026-09-05 14:34:11 +08:00` 构建，大小 `9015734` bytes，SHA-256 `EA6BFF586DE379CD502DC58D6C5EF8A99655E74634F2F254DF0D643783ACA753`。
- 安装包：`E:\Project\test\prototype\src-tauri\target\release\bundle\nsis\本地资料工作台_0.3.41_x64-setup.exe`；loader 的 Windows x64 架构及主程序同目录关系已验证。
- 阶段分支为 `codex/phase-0.3.41-preview-state`，开发前 `dev` 为 `03298ae`；实现提交及交付记录以本地 `ff-only` 合入 `dev`，合并后清理阶段分支。开发侧收口提交为 `1582d04`，安装包对应源码提交仍为 `93a6779`。
- 2026-09-05，用户确认阶段 A 的 Windows 11/Tauri/WebView2 原生验收已完成。验收确认不改变 `0.3.41` 源码、版本或已构建安装包；该版本仍为未发布本地候选。
- 没有执行阶段 B 开发、远端 CI、push、Tag 或 Release。下一步为阶段 B 的文件重定位、重命名元数据合并与删除恢复。

## 6. 阶段 B：文件操作一致性与删除恢复（0.3.42）

### 6.1 合并范围

合并 R03、R04、R05，复用 A 的上下文与同步规则，处理文件身份、元数据保留和物理操作后的结果。

### 6.2 重新定位

- 根据条目 kind 选择文件或目录模式，记录目标 ID、种类和选择请求身份。
- 取消不改资料；错误类型、重复登记路径、不可访问或危险路径保留原记录并返回明确原因。
- 提交时再次确认记录存在且仍需重定位，保留 ID、收藏、标签、分组和历史字段。
- 更新和排序后按稳定 ID 返回条目，禁止按排序前数组位置取值。
- 响应条目 ID 必须与请求一致，通过 A 的版本和上下文入口应用结果。

### 6.3 重命名与并发元数据

- 物理操作前保存受控源快照，使用现有路径和来源复核策略。
- 物理操作后在提交锁内查找当前记录，确认仍对应原来源，只更新本次影响的路径、名称、格式和文件系统元数据。
- 收藏、标签、分组、添加时间、最近记录与最近打开从提交时的当前条目保留，不从操作开始的旧条目恢复。
- 同一文件冲突的物理操作明确串行或拒绝；不同文件只读预览不被全局 busy 无条件阻塞。
- 保存失败尝试受控回滚；回滚失败返回真实 partial-success / unknown 和恢复入口，不能声称原文件未改变。
- 回滚不得覆盖原路径新建的文件，也不得操作身份已经改变的来源。

### 6.4 删除日志恢复矩阵

恢复只处置已登记操作，不自动重新执行物理删除。

| 日志状态 | 当前来源 | 预期处理 |
| --- | --- | --- |
| prepared | 原来源仍存在且一致 | 保留资料，结束或标记未完成操作，启动时不自动删除 |
| prepared | 原路径不存在 | 保留核对信息，依据可靠证据修复或展示待核对状态，不能永久忽略或直接宣称全部成功 |
| prepared | 路径存在不同来源 | 拒绝继续操作该路径，保留当前文件并提供冲突处置 |
| physical-complete | 原路径不存在，条目仍对应原来源 | 幂等清理对应索引记录与日志，保留其他资料 |
| physical-complete | 条目已重定位或路径出现新文件 | 不删除新记录或新文件，依据原操作身份核对日志 |
| 任意状态 | 索引或日志写入失败 | 保留可恢复状态，重试和重启不损坏另一份已保存数据 |

- 覆盖 prepare、物理结果、mark complete、索引保存、日志清理间的每个失败窗口。
- 已处理原文件但尚未同步时提供刷新、核对与修复，不一律提示重建空索引。
- 显式重建空索引展示影响范围，并同步处理 pending；完成后不能继续因同一日志显示警告。
- 两个 JSON 不天然具有数据库事务，必须说明持久化顺序、补偿失败和中途退出的幂等恢复策略。
- 确需扩展日志字段时明确格式版本、旧 prepared 解释和迁移备份，不把新格式静默写成旧版本。

### 6.5 文件与验证

主要文件：文件 action service、`useLibraryActions.js`、确认弹窗、`App.jsx`、`commands/library.rs`、重新定位 command、`filesystem/operations.rs`、`storage/mod.rs`、Repository、pending 与 IPC 错误契约。

- 覆盖文件 / 目录重定位、取消、错误类型、重复路径和排序后 ID 正确。
- 用可控屏障复现“开始重命名 -> 元数据修改 -> 提交”，证明新收藏等字段保留。
- 隔离夹具或测试适配器注入各持久化步骤失败，覆盖日志两状态、来源重建、条目重定位和重复恢复。
- 重建后索引、pending 文件和公开恢复状态一致；自动测试不操作真实回收站资料。
- 运行 library、contracts、operations 的相关测试和 Rust 文件操作 / 恢复检查。
- 五个版本文件统一为 `0.3.42`，本地构建 `本地资料工作台_0.3.42_x64-setup.exe`，通过 loader 校验并记录证据。
- 用户验收：失效文件修复、重命名与收藏并发、回收站删除、错误反馈和重启恢复。

### 6.6 本阶段交付记录（2026-09-05）

- R03-R05 实现提交：`2cac22352cae7ad8e5e7095ad1b3764b0935bd29`。主索引保持 v5，删除日志 v1 备份迁移至 v2；恢复规则与回退要求见 `docs/file-operation-recovery.md`。
- 前端相关定向检查 19 项与 Rust 文件操作/恢复检查 12 项通过，fmt/check/check-tests/严格 clippy 通过。按用户补充要求未执行完整测试组合；Edge 仅对合成恢复界面与确认框做布局检查。
- 五个版本文件与主程序均为 `0.3.42`，NSIS 于 `2026-09-05 15:19:43 +08:00` 构建，大小 `9010160` bytes，SHA-256 `1697CA4522073E472FE1C045FD14D708153B0701AF7FF86EEC0FDF2D33306CCA`；x64 loader 校验通过。
- 安装包：`E:\Project\test\prototype\src-tauri\target\release\bundle\nsis\本地资料工作台_0.3.42_x64-setup.exe`。
- 阶段分支 `codex/phase-0.3.42-file-recovery` 从 `dev` 的 `99837ee` 开始；源码与安装包记录 `77b2f76` 已通过 ancestry 检查并以本地 ff-only 合入 `dev`。随后同步本阶段收口状态并清理已合并阶段分支，打包源码保持 `2cac223`。
- 2026-09-05，用户确认阶段 B 原生验收完成；未提供具体 Windows/WebView2 版本或样本明细，不补填环境字段。验收登记不改变源码及 `0.3.42` 安装包。
- 阶段 B 收口时，阶段 C 尚未开始，未执行 push、Tag、Release 或上传；`0.3.42` 为未发布本地候选。阶段 C 的后续状态见第 7 节。

## 7. 阶段 C：正文索引、摘要与搜索执行（0.3.43）

### 7.1 合并范围

合并 R02、R11 和 ARC02 的正文搜索部分，修复缓存往返一致性，并使正文查询离开同步 IPC 执行路径。

### 7.2 统一编码与大小边界

- 区分源字节数、解码后的 UTF-8 字节数、字符数、总正文大小、条目数与 JSON 文件大小。
- 默认保持当前缓存读取兼容边界：源文件最多 2 MiB、单条解码后 UTF-8 最多 2 MiB、单条最多 100 万 Unicode 字符、总正文最多 64 MiB、最多 20,000 条、JSON 文件最多 72 MiB。
- 超出单条限制只跳过当前资料并给出原因，不能先写出重启后无法读取的缓存。
- 同步、重建、写入前和启动读取复用相同规则；保存函数不得绕过结构与条目校验。
- 对此前写出的超限条目，在受控备份后尽量保留其他有效缓存，说明隔离数量；根结构损坏或未知版本仍进入明确恢复流程。
- 不为修复正文缓存而重置主索引、收藏、分组或原文件；错误只提供类别，不暴露正文与真实完整路径。
- 记录旧候选回退时的缓存兼容行为，优先保持正文缓存 v1，确需更改格式时显式版本化。

### 7.3 摘要坐标

- 按实际前缀字符串的 Unicode 字符数计算偏移，最终摘要与命中范围使用同一坐标系。
- 覆盖开头、中间、末尾、无前缀、多个命中、长命中截断、中文和非 BMP 字符。
- 所有范围落在最终摘要内，命中数、截断标识和实际高亮内容一致。

### 7.4 正文查询与缓存状态

- 正文查询移入受控异步 / blocking worker，不在同步 command 中复制和扫描整份正文。
- 查询使用共享只读快照，同一结果的内容和 source revision 一致，不持有写锁扫描全文。
- 请求携带身份和过期处理，输入变化、清除、重建、卸载后，迟到响应不得覆盖新状态。
- 限制并发查询与待执行请求，防抖不能替代任务数量控制，超时和取消可诊断。
- 保留 Rust regex 的表达式、程序大小、每文档命中数和摘要上限，不迁回主线程 JavaScript 扫描。
- 增量更新处理新增、变化、失效和移除，剩余空间正确扣除已删除或替换正文。
- 清除、重建和后台同步有明确顺序，source revision 不回退；取消重建保留此前可用快照。
- 明确“清除正文索引”的生命周期和后续自动更新规则，不将一次清除描述为永久禁用索引。

### 7.5 文件与验证

主要文件：`storage/content_index.rs`、`content_search.rs`、`preview/text.rs`、正文 command / 事件调度、`useContentIndexController.js`、`LibraryPanelParts.jsx`、正文设置与 IPC 契约。

- 复现 80 万汉字 GB18030 样本：源数据 1,600,000 字节，解码 UTF-8 为 2,400,000 字节。新行为明确跳过该超限项，其他正文可保存并重启加载。
- 覆盖各边界前后值、总容量、重建 / 增量写入，以及保存后重新初始化的往返检查。
- 检查真实渲染的高亮字符，不只断言后端数字范围。
- 覆盖搜索与清除 / 重建乱序、取消、并发上限和缓存写入失败。
- 运行 content、相关 contracts / preview 和 Rust 正文测试，记录近总容量时的耗时与内存观察，不伪造性能保证。
- 五个版本文件统一为 `0.3.43`，本地构建 `本地资料工作台_0.3.43_x64-setup.exe`，通过 loader 校验并记录证据。
- 用户验收：中文正文、摘要、退出重开、清除 / 重建、快速输入与恢复。

### 7.6 本阶段交付记录（2026-09-05）

- R02、R11 及正文查询顺序和性能落在 `d59471d`；重建入口先废弃旧任务再读取主索引快照的最终修正为 `e8b08f127ff2a892966d9ee7e2f190fe95c6e3fc`。主索引保持 v5，正文缓存保持 v1，新增 cacheRevision 仅在内存和 IPC 中使用。
- 自动验证：Rust 正文定向 16 项、前端正文模型/契约/Hook/实际摘要渲染 9 项、相关 IPC/预览定向 23 项和 57 个 command parity 通过；Rust fmt/check/check-tests/严格 clippy、五文件版本核对通过。最终顺序修正后仅复查受影响的 Rust 编译/严格 clippy/格式并重新打包。
- 64 MiB 合成缓存 debug 观察：重建 4214 ms、1000 次共享快照获取 38 us、查询 1607 ms、增量保存 2681 ms；测试进程峰值工作集约 208 MiB，不代表正式应用查询内存或用户设备性能保证。
- Edge 在 1280x800、720x800 检查正文设置和实际摘要组件；仅使用合成夹具，临时服务 49343 和浏览器已关闭，端口已释放。该记录属于开发侧验证，用户原生验收另见本节确认记录。
- 五个版本文件、主程序及 NSIS 均为 `0.3.43`；最终安装包在 `2026-09-05 16:09:18 +08:00` 构建，大小 `9050302` bytes，SHA-256 `81669427911CB211B9376E91F0BB59AA9EAC7B95E7315024BDE79947287FACE5`。
- 安装包：`E:\Project\test\prototype\src-tauri\target\release\bundle\nsis\本地资料工作台_0.3.43_x64-setup.exe`；loader 的 x64 架构和主程序同目录关系已通过校验。保留上一 `0.3.42` 候选。
- 分支 `codex/phase-0.3.43-content-search` 从本地 `dev` 的 `008ca291baba261db2f446682a5475f3a1feb680` 创建，代码、文档和打包均在该分支完成；实现及安装包证据提交 `d474aa7` 已经 ancestry 检查和 `git merge --ff-only` 合入本地 `dev`。收口记录 `efcb361` 已同步并安全清理阶段分支；`main` 保持 `03298ae`。阶段 C 开发侧与用户原生验收均已完成，未进行后续阶段开发、push、Tag 或 Release。
- 2026-09-05，用户明确回复“已验收”，阶段 C 的 Windows 11/Tauri/WebView2 原生验收登记为完成。未提供具体系统/WebView2 版本或样本明细，不补填环境字段。验收文档通过独立分支 `codex/phase-0.3.43-acceptance` 同步到本地 `dev`，不改变源码、版本或现有 `0.3.43` 安装包；该版本已创建 `v0.3.43` Tag 并发布 GitHub Release，包含 Windows x64 NSIS 安装包和便携 ZIP。

## 8. 本轮完成核对表

- [x] R01：真实 PDF.js 完整读取大 PDF，资源请求与长度一致。
- [ ] R02：缓存写入与重启约束一致，超限项不损坏其他有效正文。
- [ ] R03：删除中间状态可处置，恢复 / 重建不遗留相同 pending 警告。
- [ ] R04：重命名保留并发用户元数据。
- [ ] R05：文件 / 目录重定位正确，排序后响应对应请求 ID。
- [x] R06：同步保留有效目录子项选择和预览。
- [x] R07：迟到目录结果不覆盖新导航。
- [x] R08：有限重试、可取消、可手动恢复。
- [x] R09：无关修改不导致有效预览漏记，真正过期任务仍拒绝。
- [ ] R11：省略号、Unicode 与最终摘要坐标一致。
- [ ] ARC02：正文查询异步、共享只读数据和性能证据可追溯。
- [ ] ARC03：字段契约、controller 类型、交互和 command 业务可检验。
- [ ] A-C 的 `0.3.41`、`0.3.42`、`0.3.43` 分别产出本地 Windows x64 NSIS。
- [ ] A-C 分别在独立阶段分支开发，每阶段通过门禁后先本地快进并入 `dev`。
- [ ] 每阶段记录五个版本文件、源码提交、安装包路径 / 大小 / SHA-256、loader 与 Git 收口。
- [ ] 最终用户验收已确认，剩余风险和远端状态按实际证据记录。

## 9. 每阶段固定回报

阶段最终回报和 `PROJECT_PROGRESS.md` 至少包含：

1. 阶段、候选版本、对应 R / ARC 编号与开发状态。
2. 实际修改模块、用户行为、契约和磁盘格式影响。
3. 检查命令、结果、未执行项及影响，必要回归和性能证据。
4. 五个版本文件一致性与最终源码提交。
5. 本地 NSIS 绝对路径、构建时间、大小、SHA-256、loader 结果。
6. 阶段分支、dev 合并前后 HEAD、快进和分支清理结果。
7. 原生验收、远端 CI、push / Tag / Release 的真实状态。
8. 尚存阻塞与后续任务。

计划、注释、测试数量和安装包存在本身不能替代行为证据。阶段 A-C（`0.3.41` 至 `0.3.43`）的开发侧交付及用户原生验收已完成；`v0.3.43` 已创建 Tag 并发布 GitHub Release；当前没有阶段 C 之后的执行计划。
